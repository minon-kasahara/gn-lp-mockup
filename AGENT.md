# Agent 行動ルール — 株式会社G&N LP制作プロジェクト

このファイルは本プロジェクトにおけるAgent（Claude）の行動規範を定めるものです。
ユーザーとの会話を通じて随時更新していきます。

最終更新: 2026-05-07（§14 Studio 仕様の管理ルール追加）

> 💡 **新規セッションの方へ**: まず [README.md](README.md) を読んでください。プロジェクトの全体像と現状ステータスがまとまっています。

---

## 1. プロジェクト概要

- **クライアント**: 株式会社G&N
- **案件**: ランディングページ（LP）制作
- **最終納品形態**: [Studio](https://studio.design/ja) 上で作成したLP
- **目的**: StudioというノーコードWeb制作ツール上にアウトプットできる設計を行う
- **作業環境**: 各セッションは **Claude デスクトップアプリ（macOS）** 上で動作。CLI ではない（環境変数設定に `launchctl setenv` が必要なのはこのため）

## 2. ディレクトリ構成と役割

プロジェクトルート: `01_LP制作_202604/`

| ディレクトリ | 役割 | Agentの権限 |
| --- | --- | --- |
| `01_input/` | ユーザー提供のインプット資料（ヒアリングシート・特典資料等） | 読み取りのみ。変更・削除しない |
| `02_work/` | Agent作業領域。設計書・構成案・メモ等を自由に作成可 | 自由に作成・更新・削除可 |

※ 当初ユーザー指示は `01_work` だったが実ディレクトリは `02_work`。以降 `02_work` を使用（要確認で確定したら本項を更新）。

### 02_work内の成果物（現状）
| ファイル / ディレクトリ | 役割 | 新規セッション読み込み優先度 |
| --- | --- | --- |
| `README.md` | **引き継ぎエントリーポイント**。全体像・現状ステータス・推奨手順 | ★1番最初 |
| `AGENT.md` | 本ファイル。行動規範・ワークフロー | ★2番目 |
| `activity_log.md` | **セッション間活動ログ**（2セッション並行時の差分対策） | ★3番目 |
| `tasks.md` | **タスク管理台帳**（全タスクの永続記録） | ★4番目 |
| `questions.md` | **ユーザーへの要確認事項**（回答欄付き） | ★5番目 |
| `notes_input.md` | インプット資料の要約（元ファイル再読込不要） | ★6番目 |
| `design_rules.md` | **確定デザイン要素の蓄積**（常時参照・随時更新） | ★7番目 |
| `design_references.md` | **参考サイト8件の分析結果**（壁打ち前必読） | ★8番目 |
| `mockup/INDEX.md` | モックアップの版履歴・確定状況 | モックアップ作業時 |
| `mockup/drafts/` | デザイン壁打ち用のドラフト版HTMLモックアップ（複数世代保持） | モックアップ作業時 |
| `mockup/final/` | 最終確定版HTMLモックアップ | 確定後 |
| `studio_guide/INDEX.md` | **Studio実装指示書の版履歴・作成状況** | 指示書作成時 |
| `studio_guide/drafts/` | Studio実装指示書のドラフト版 | 指示書作成時 |
| `studio_guide/final/` | **Studio実装指示書の最終確定版（Agentの最終アウトプット）** | 最終フェーズ |

## 3. インプットデータの扱い

現在 `01_input/` に格納されているファイル:
- `G&N様_LP hearing sheet.xlsx` — ヒアリングシート
- `特典紹介資料.pptx` — 特典紹介資料

### ルール
- 必要なタイミングで読み取り、内容を理解する
- インプットファイルは編集・上書きしない
- 内容を引用・参照する場合は出典（ファイル名・シート名・スライド番号等）を明記する
- 資料から読み取った重要事項は `02_work/` 内のメモファイル（例: `notes_input.md`）に要約して蓄積する

## 4. Studio納品を前提とした設計原則

Studioはノーコードのビジュアル編集ツールのため、最終的な実装はユーザーが手作業で行う想定。よってAgentの出力は「Studio上で再現可能な粒度」で設計する。

### Studio側の制約（2026-04時点、公式ヘルプ・ブログより確認済み）
- ❌ **ページ単位のHTML直接インポートは不可**（Embedボックスでの部分埋め込みのみ可能）
- ❌ **ページHTMLの直接編集・コード書き出しは不可**
- ❌ **外部からデザイン/構造をAPIで書き込む手段は提供されていない**（Data Connect APIは取得専用）
- ✅ **Figmaからのインポートは可能**（「Figma to Studio」プラグインでコピペ）
  - ⚠️ **レイアウトのみ引き継ぎ**。Figmaのプロトタイプ/アニメーション/ホバー/クリック動作は**引き継がれない**
- ✅ **WordPress XMLインポートは可能**
- ✅ **Embedボックス**: 部分的なHTML/scriptの埋め込みは可
- ✅ **Data Connect API**: Notion / microCMS / Airtable / カスタムAPI から動的データ取得可（取得のみ、書き込み不可）

### Studio側のアニメーション機能
- **トランスフォーム**: Translate / Rotate / Scale / Skew
- **トリガー**: 表示時 / スクロール到達 / ホバー / クリック
- **スクロール連動**: パララックス、スクロール量連動の変形
- **Lottie連携**: 複雑なアニメをJSONで取り込み
- → **どのワークフロー案でもアニメーションはStudio上で実装する**（Figma経由でも引き継がれないため）

### 出典
- [Studioサイトで HTML の編集や追加、出力はできますか？ | Studio Help](https://help.studio.design/ja/articles/8864939)
- [Data Connect API（API連携） | Studio Help](https://help.studio.design/ja/articles/6500751)
- [API連携機能リリース | Studio Blog](https://studio.design/ja/blog/api-integration)

### 設計原則
- **ブロック単位で分解**: Studioの「ボックス」「テキスト」「画像」等の標準コンポーネントで組める単位に分解する
- **レスポンシブ前提**: PC/タブレット/モバイルでの表示崩れを避けるため、セクションごとにブレークポイント方針を明示
- **独自JSや複雑なアニメーションに依存しない**: Studio標準機能で実装できる範囲を前提とする
- **アセット指示を明確に**: 画像・アイコン・フォントについて、用意物か既存素材流用かを明記
- **具体値を明示**: カラーはHEX、サイズは px / %、フォントは Studio で利用可能なものを指定

## 4.5 採用ワークフロー（2026-04-24 確定）

**HTMLプレビューを伴うStudio手実装方式** を採用する。HTMLはStudioへのインポート用ではなく、**視覚的なデザイン壁打ち用のモックアップ**として使う。

### フェーズ構成

| フェーズ | 内容 | Agentの成果物 | ユーザーのアクション |
| --- | --- | --- | --- |
| **① 要件整理** | ヒアリングシート・特典資料を読み込み、LPの目的・ターゲット・訴求軸を確定 | 要件整理メモ | 内容確認・補足 |
| **② 構成検討** | セクション構成・情報設計・ワイヤーフレームを議論 | 構成案・ワイヤーフレーム（テキストorHTML） | フィードバック |
| **③ デザイン壁打ち** | HTMLモックアップを生成してビジュアル案を可視化、対話で反復修正 | `02_work/mockup/` にHTMLファイル | ブラウザで確認・指示 |
| **④ デザイン確定** | 最終デザインを固定 | 確定版HTMLモックアップ | 最終承認 |
| **⑤ 実装指示書作成** | Studio実装用の詳細指示書を作成 | Studio実装指示書（セクション別・ブロック単位） | 受領 |
| **⑥ Studio実装** | — | 指示書に対する疑問点への回答・修正指示 | Studio上で手動実装 |
| **⑦ 実装レビュー** | 実装結果を確認し差異があれば指示書を修正 | 修正指示 | 反映 |

### HTMLモックアップの位置づけ
- **目的**: デザインの可視化・壁打ち専用。Studioインポートは前提としない
- **保存先**: `02_work/mockup/` 配下（版管理のため連番やバージョン付与）
- **技術制約**: Studioの標準機能で再現可能な表現に留める（独自すぎるアニメや複雑な動的処理は避ける）
- **再現性担保**: HTML側で使う色・フォント・サイズ・間隔等は、後工程の実装指示書と一致させる

### Studio実装指示書の要件
最終成果物として、以下を含む指示書を `02_work/` に作成する:
- **セクション単位の構成図**（HTMLモックアップとの対応関係を明記）
- **ブロック単位の要素分解**（Studioの「ボックス」「テキスト」「画像」等で組める粒度）
- **デザイントークン**: カラー(HEX)・フォント・フォントサイズ・行間・余白の具体値
- **レスポンシブ仕様**: PC/タブレット/モバイルでの崩れ方の指示
- **アニメーション指定**: Studio標準機能（Translate/Rotate/Scale/Skew × トリガー）での実装仕様
- **アセット一覧**: 画像・アイコン・Lottie等の手配状況と配置指示
- **操作手順の補足**: Studio特有の設定箇所（カスタムコード・Embed・Data Connect等）があれば個別指示

## 5. Agentの振る舞い

### 新規セッション開始時の手順
1. `README.md` を読む（全体像と現状ステータスを把握）
2. 本ファイル `AGENT.md` を読む（行動規範確認）
3. **`activity_log.md` を読む**（他セッションの直近活動を把握、最低直近10-20エントリ）
4. **セッション識別子を決め、`activity_log.md` に `[SESSION-START]` エントリを追記**（後述 §7）
5. **🎨 Gemini 画像生成セットアップ確認（必須）**:
   - `activity_log.md` を `[DONE].*Gemini.*セットアップ` で grep / 検索し、**自セッション識別子で完了記録があるか確認**
   - **未完了の場合**: ユーザーに「Gemini 画像生成セットアップを実行します」と通知し、`setup_gemini.md` の Step 1〜4 を実行（コピペ3コマンド + Claude Code 再起動 + 完了ログ記録）
   - **完了済みの場合**: スキップして次へ
   - 既知の完了状況: **azalea = 完了**（2026-04-27）／**kasahara = 未確認・初回必須**
6. **`tasks.md` を読む**（進行中・未着手タスクの把握）
7. **`questions.md` を読む**（ユーザー回答の有無・未回答の残件を確認）
8. `questions.md` に**事前回答が書き込まれていれば**、内容を `design_rules.md` や `notes_input.md` に反映
9. 反映完了した質問は `questions.md` の「完了済み質問」セクションに移動
10. `notes_input.md` と `design_rules.md` を読む（詳細コンテキスト）
11. セッション冒頭に**残タスクと未回答質問を要約してユーザーに提示**し、当面の作業に必要な回答を優先的に促す

### 質問管理のルール（`questions.md`）
質問ファイルは**Agent側で能動的に運用**する。

**① 読み込みタイミング**
- セッション開始時（上記手順3）
- ユーザーから新しい指示・情報が来た時（該当する未回答項目がないか確認）
- デザイン壁打ち・実装指示書作成など**次フェーズに進む直前**（未回答の阻害要因がないか確認）

**② 回答が得られた時の処理（セッション内対話で回答があった場合）**
1. ユーザーの回答内容を `questions.md` の該当項目の「回答」欄に転記する
2. ステータスを `⏳ 未回答` → `✅ 回答済み` に書き換える
3. 回答内容を適切な場所に反映する:
   - デザイン関連 → `design_rules.md`
   - インプット情報の追加・修正 → `notes_input.md`
   - ワークフロー・ルール関連 → 本ファイル `AGENT.md`
4. 反映完了後、その質問を「完了済み質問」セクションに移動
5. 「最終更新」日付を書き換える

**③ 未回答項目の確認タイミング（能動的にユーザーに促す）**
- セッション開始時: 未回答の一覧を提示
- タスク着手前: そのタスクに必要な未回答項目があれば先に確認
- 壁打ち中に判断が必要になった時: 該当する未回答項目を参照し、必要なら追加質問
- 質問が埋もれないよう、**会話の区切り・節目で残件を再提示**

**④ 新しい質問が発生した時**
- 壁打ち・実装検討中に新たな不明点が出たら、`questions.md` に追記
- 優先度カテゴリ（🔴壁打ち前必須 / 🟡並走可能 / 🟢後続）を適切に付与
- ユーザーに即確認すべきものか、後回しにできるものかを判断して提示タイミングを決める

### 基本姿勢
- 日本語で簡潔に応答する
- 不明点・前提が曖昧な点は推測で進めず、ユーザーに確認する（確認事項は `questions.md` に追記）
- 大きな方針変更・不可逆な操作は必ず事前にユーザーの合意を取る
- ファイル編集・作成は `02_work/` 内に限定（明示指示がない限りそれ以外を書き換えない）

### このファイル（AGENT.md）の更新
以下のタイミングで本ファイルを更新する:
- ユーザーから新しい指示・前提・制約が提示されたとき
- 作業の中で有用な知見・運用ルールが確立したとき
- ディレクトリ構成や成果物一覧に変更があったとき
- 更新時は「最終更新」の日付を必ず書き換える

### `design_rules.md` の扱い
- 壁打ちでデザイン要素（カラー・タイポ・余白・アニメーション等）が**確定したタイミングで必ず追記**する
- HTMLモックアップ生成前に `design_rules.md` を参照し、確定要素は厳守する
- 壁打ち中に暫定決定が出た場合も、確定前提の「未確定」扱いのまま保持し、確定時に書き換える

### Studio実装指示書の保管ルール（`studio_guide/`）
**Agentの最終成果物**。デザイン壁打ちで確定したHTMLモックアップに基づき、ユーザーがStudio上で手動実装するための詳細指示書を作成する場所。

**① ディレクトリ**
- `studio_guide/drafts/` — 指示書ドラフト版。上書きせず版番号付与
- `studio_guide/final/` — 最終確定版（ユーザーが実装時に参照する最重要ファイル）
- `studio_guide/INDEX.md` — 版履歴・セクション別の作成状況

**② ファイル命名規則**
- 全体版ドラフト: `v{連番}_{YYYYMMDD}_guide_full.md`
- セクション別ドラフト: `v{連番}_{YYYYMMDD}_guide_{section}.md`（section = fv / problem / solution / service / results / vc / benefit / case / cta / company / faq / common）
- 並列案: 末尾 `_A` / `_B`
- 確定版: `guide_full_final.md`, `guide_{section}_final.md`

**③ 指示書の作成条件（必ず事前に満たす）**
- `mockup/final/` に対応するHTMLモックアップ確定版が存在する
- `design_rules.md` の主要項目（カラー・タイポ・余白・コンポーネント）が確定
- `questions.md` Q1-Q3 および必要な法務確認（Q6）が解消済み
- コピー・画像等アセットが確定

**④ 指示書の推奨構成（セクションごと）**
1. 概要・対応モックアップ
2. レイアウト（Studio ブロック構造 / レスポンシブ）
3. テキストコンテンツ（確定コピー）
4. デザイン指示（色HEX / フォント / 余白 px単位）
5. 画像・アイコン（ファイル名・配置・代替テキスト）
6. アニメーション（Studio標準機能での設定）
7. リンク・CTA設定
8. Studio特有の注意点（カスタムコード・Embed・Data Connect）
9. テストチェックリスト

**⑤ 版管理**
- ドラフト作成・更新の都度、`studio_guide/INDEX.md` の「版履歴」「作成状況」を更新
- 確定版昇格時: `final/` にコピー → INDEX.md 更新 → activity_log.md に `[DONE]` 記録

### モックアップ保管のルール（`mockup/`）
デザイン壁打ちで生成するHTMLモックアップは、**都度必ず `mockup/` 配下に保存**し、版管理する。

**① ディレクトリ**
- `mockup/drafts/` — 壁打ち中のドラフト。**作成の都度ここに保存**（上書きせず新規ファイル化）
- `mockup/final/` — ユーザーが確定を宣言したバージョンのみ保存

**② ファイル命名規則**
- ドラフト: `v{連番}_{YYYYMMDD}_{対象}_{短い説明}.html`
  - 例: `v01_20260424_fv_initial.html`, `v02_20260424_fv_color_adjust.html`
- 並列案: 同連番で末尾に `_A` / `_B` を付与
  - 例: `v04_20260425_fv_A.html`, `v04_20260425_fv_B.html`
- 全体通しの場合は対象を `full` とする
  - 例: `v10_20260428_full_first.html`
- 確定版: `{対象}_final.html`
  - 例: `fv_final.html`, `lp_full_final.html`

**③ 版履歴の記録**
- モックアップを作成・更新したら、必ず `mockup/INDEX.md` の「版履歴」表に1行追記する
- 確定版を作成したら、`mockup/INDEX.md` の「確定状況」表を更新する

**④ 運用原則**
- **既存ファイルを上書き編集しない**（版が追えなくなるため）。常に新しいバージョン番号で保存
- ただし、同一セッション内でのごく軽微な修正はユーザー合意のもと例外可
- HTMLモックアップには `design_rules.md` の確定事項を厳守する
- HTMLは**壁打ち用のプレビュー**であり、Studioインポートは不可。ユーザーはブラウザで視認確認する

**⑤ 確定版への昇格手順**
1. ユーザーが「このバージョンで確定」と宣言
2. 該当の drafts ファイルを `final/` に `{対象}_final.html` でコピー
3. `mockup/INDEX.md` の「確定状況」表を更新
4. `design_rules.md` に未記載の確定要素があれば反映

### タスク管理のルール（`tasks.md`）
プロジェクトの全タスクは `tasks.md` に永続記録する。セッション内の TodoWrite は作業中の詳細進捗トラッキング用（ephemeral）で、両者を併用する。

**① 新規タスクの起票タイミング（都度起票）**
- ユーザーから新たな作業依頼が出た時
- 壁打ち・実装検討中に新たな作業項目が判明した時
- 質問が回答されて具体的な作業に落ちた時
- 次フェーズへ進む準備が必要になった時
- ブロッカーが発生した時（解消タスクとして起票）

**② タスクの記録形式**
- ID付与: `T{連番3桁}`（取下げ後も番号は維持、欠番化しない）
- 優先度: 🔴必須・🟡重要・🟢通常・⚪後回し可
- ステータス: ⏸未着手 / ▶進行中 / ✅完了 / ❌取下げ
- 担当: ユーザー / Agent / 両方
- 関連: 関連する質問ID・ファイル等を明記

**③ ステータス更新**
- 着手時: ⏸未着手 → ▶進行中
- 完了時: ▶進行中 → ✅完了（完了済みセクションへ移動）
- 中断時: ▶進行中 → ⏸未着手（補足欄に中断理由）
- 不要化: ❌取下げ（理由を必ず補足）

**④ セッション内 TodoWrite との関係**
- セッション内の細かい進捗: TodoWrite（ephemeral）
- セッションまたぎの永続記録: `tasks.md`
- **作業区切りまたはセッション終了前に、TodoWrite の完了内容を `tasks.md` に反映**する

**⑤ 会話ケイデンス（重要）— ユーザーとの対話での残件確認**
- **セッション冒頭**: 未着手タスクと未回答質問のサマリを提示
- **作業着手前**: ブロッカーとなる未解消項目があればユーザーに確認
- **作業完了時・節目**: 完了タスクの要約 + 次に着手するタスクの提示
- **応答末尾（残件がある場合）**: 「🔴必須」優先度の未解消項目が残っていれば1-3項目程度をコンパクトに明示
- 長時間ユーザー返答を待つ状態のタスクは、会話の節目で**静かに再提示**（毎回くどくならないよう量を調整）
- 埋もれそうな項目は「未回答継続中」としてフラグを立てる

**⑥ 残タスク・質問のユーザー向け表示ルール（重要）**

ユーザーが一目で内容を把握し、短く回答できる形式で提示する。**タスクIDや質問IDだけを並べず、必ず中身を展開する**。

- **🔴必須の質問**: 質問本文＋選択肢（A/B/C/...）を**インラインで全文表示**する。最後に「Q1=B / Q2=A」形式の短答テンプレートを提示
- **🟡並走可能の質問**: 表形式で「# / 質問 / 選択肢」を1行にまとめる（コンパクト）
- **🟢後続の質問**: 表形式で「# / 質問」のみ（選択肢は省略可）
- **タスクのみ（質問に紐づかないもの）**: 担当・期日・関連を簡潔に示す
- 長すぎる補足（サブコピー全文など）は必要な場合のみ明示、冗長にならないよう調整
- 優先度のアイコン（🔴🟡🟢⚪）を維持し、視覚的にグルーピング

## §7 2セッション並行運用ルール（**超重要**）

本プロジェクトは**2人のユーザーが別アカウントで独立したセッションを持ち、同じディレクトリを参照しながら作業**する運用。セッション間でコンテキスト差分を発生させないため、以下のルールを厳守する。

### 7.1 基本原則

| 原則 | 内容 |
| --- | --- |
| **脳内保持禁止** | セッション内会話で決まった事項は**必ず即座に関連ファイルに書き込む**。ユーザーとの口頭合意だけで済ませない |
| **読み直し最優先** | ファイル編集・参照する直前に、必ず**最新の状態を読み直す**（他セッションが更新している可能性） |
| **活動ログ必須** | ファイル変更の前後で必ず `activity_log.md` に記録する |
| **セッション識別** | 各セッションは開始時に自己同定し、以後の全ログ記録にその識別子を付ける |
| **競合時はユーザー確認** | 他セッションの記述と矛盾する情報が来た時は、自動マージせず**ユーザーに確認** |

### 7.2 セッション識別子の扱い
- セッション開始時、Agentがユーザーに識別子を尋ねる（既存の `activity_log.md` を見て衝突しないものを選ぶ）
- 例: ユーザーのメール短縮形 / ロール名（`designer`, `planner` など）
- 1セッション内でのAgent発言・ファイル更新・ログ記録に**常に同じ識別子を使う**

### 7.3 活動ログへの記録タイミング

| タグ | タイミング | 内容 |
| --- | --- | --- |
| `[SESSION-START]` | セッション開始時 | 自己同定＋直近で何をする予定か |
| `[INTENT]` | ファイル編集・重要判断の直前 | これから何をするかの宣言 |
| `[DONE]` | ファイル編集・重要判断の直後 | 何をしたかの要約＋変更ファイル |
| `[BLOCKER]` | 待ち状態・ブロッカー発生時 | 何が止まっているか、誰の対応待ちか |
| `[HANDOFF]` | 他セッションへ引き継ぐ時 | 引き継ぎ事項・注意点 |
| `[SESSION-END]` | セッション終了時 | 残作業サマリ、次に想定されるアクション |

### 7.4 編集前のチェックリスト（毎回）

ファイルを編集する前に:
1. 対象ファイルを Read で読み直して最新状態を取得
2. `activity_log.md` を確認し、他セッションの直近 `[INTENT]` / `[DONE]` を確認
3. 競合があればユーザーに確認し、判断を仰ぐ
4. 編集前に `[INTENT]` エントリを活動ログに記録
5. 編集実行
6. 編集後に `[DONE]` エントリを活動ログに記録

### 7.5 編集中の衝突回避
- **同一ファイルへの同時編集**: 活動ログで `[INTENT]` を確認。他セッションが30分以内にINTENTを出していたら、完了を待つかユーザー確認
- **タスクの割当**: `tasks.md` に担当セッション識別子を記載し、他セッションは手を出さない
- **モックアップの同時生成**: ファイル命名規則で版番号＋末尾 `_A` / `_B` をセッションごとに使い分け、物理的に衝突しない運用

### 7.6 ユーザーへの報告ルール
- 会話冒頭で「セッション識別子」「他セッションの直近活動」を簡潔に伝える
- 応答末尾で、他セッションによる更新があった場合は要約して周知
- 自分のセッションが行った変更は必ずログ＆ファイルに反映してから応答を終える

### 7.7 セッション識別子の決定フロー
新規セッション開始時:
1. `activity_log.md` を読み、現在使われている識別子を確認
2. ユーザーに「今回のセッション識別子は何にしますか？」と聞く（候補を提示可）
3. 決まった識別子で `[SESSION-START]` エントリを書き込む
4. 以降の全ログに同じ識別子を使用

## §9 デザイン壁打ちのルール（2026-04-24 確定）

### 壁打い方式（ユーザー指示により確定）

**デザインサンプルフォルダ（`01_input/デザインサンプル/`）内の各サイトのトンマナを元に、1サイト1案のデザイン案を作成してユーザーに確認する。**

#### 対象サイト（8件）とフォルダ名

| # | サイト | フォルダ名 | 特徴（design_references.md より） |
|---|---|---|---|
| 1 | caroa.jp/design | `https:  caroa.jp design` | クリーム/ベージュ・テラコッタ・大イラスト・フロー図 |
| 2 | castme.jp | `https:  castme.jp ` | マルチグラデーションロゴ・白ベース・顧客ロゴ多段 |
| 3 | ecology-net.com | `https:  ecology-net.com ` | グリーン主軸・3カラムイラスト・数字訴求 |
| 4 | jp.is-kagami.com | `https:  jp.is-kagami.com ` | 白→ライトブルーグラデ・3Dカード・濃紺ゴシック |
| 5 | lp.dginvoice.jp | `https:  lp.dginvoice.jp ` | 白+ネイビー+赤・3Dバブル・信頼バッジ |
| 6 | muroom.chicabi.jp | `https:  muroom.chicabi.jp #about` | 白×濃紺×淡ブロブ・カテゴリ小ラベル・余白多め |
| 7 | newgrads.hubble-docs.com | `https:  newgrads.hubble-docs.com ` | 白+ブルー波線・問いかけキャッチ・中庸テンション |
| 8 | www.opexpark.co.jp | `https:  www.opexpark.co.jp ` | 白+濃紺+オレンジCTA・英語見出し・4枚カード |

#### 手順

1. 対象セクションの壁打ちを始める前に、**各サイトのスクリーンショットを読み込んでトンマナを把握する**（`design_references.md` で既分析済みのため基本は参照のみ、必要時に画像再確認）
2. 8サイトそれぞれのトンマナに準拠したHTMLモックアップを1案ずつ作成する
3. ファイル名: `v{連番}_{YYYYMMDD}_{セクション}_{サイト略称}.html`
   - 例: `v03_20260424_fv_is-kagami.html`, `v03_20260424_fv_muroom.html`
4. 全案をユーザーに提示し、フィードバックを受ける
5. 選択・統合・修正指示に基づいて次版を作成する

#### サイト略称（ファイル名用）

| サイト | 略称 |
|---|---|
| caroa.jp | `caroa` |
| castme.jp | `castme` |
| ecology-net.com | `ecology` |
| jp.is-kagami.com | `kagami` |
| lp.dginvoice.jp | `dginvoice` |
| muroom.chicabi.jp | `muroom` |
| newgrads.hubble-docs.com | `hubble` |
| www.opexpark.co.jp | `opex` |

#### 注意事項

- 各案はG&NのLPコンテンツ（確定コピー・実績数字・セクション構成）を使いつつ、参考サイトのトンマナ（色・書体・レイアウト・装飾スタイル）を再現する
- 参考サイトのコンテンツをそのまま流用しない（あくまでトンマナの参考）
- スクリーンショット読み込みは必要なものだけ（コンテキスト節約のため代表画像のみ）

---

## §10 HTMLプレビューのルール（2026-04-27 確定）

> ⚠️ **適用範囲**: 本ルールは **kasahara セッション側でのみ運用**する（プレビューサーバーが kasahara/minon 環境に構築されているため）。
> - **kasahara 側**: 下記の rsync 同期＋プレビューURL案内を全て実施
> - **azalea 側**: 本 §10 の rsync 同期および URL 案内は**行わない**。HTMLファイル作成・編集後は、応答に「📂 保存先（Google Drive）」のパスのみ明記すれば足りる
> - 各セッションは自身の識別子（`activity_log.md` の `[SESSION-START]`）に応じて、本ルールを適用するか判断する

### プレビューサーバーの仕組み

Google Drive の FUSE ファイルシステム制限により直接サーブできないため、以下の構成を採用。

| 役割 | パス |
|---|---|
| 配信元（ローカルコピー） | `/tmp/gn-preview/` |
| サーバースクリプト | `/Users/minon/gn-serve.py` |
| launch.json | `.claude/launch.json`（`python3 /Users/minon/gn-serve.py`、port 3333） |
| Google Drive 実ファイル | `共有ドライブ/01_顧客・プロジェクト/g_株式会社G＆N/01_LP制作_202604/` |
| シンボリックリンク | `~/gn-lp` → Google Drive プロジェクトルート |

### HTMLファイルを編集・作成したときの必須手順

1. Google Drive 上のファイルを編集・保存する（通常通り）
2. **必ず `/tmp/gn-preview` に同期する**（下記コマンド）
3. ユーザーへの応答にプレビューURLと保存先を明示する

**同期コマンド（ファイル編集後に毎回実行）:**
```bash
rsync -a --copy-links "/Users/minon/gn-lp/03_minon-work/" /tmp/gn-preview/03_minon-work/ && \
rsync -a --copy-links "/Users/minon/gn-lp/02_work/mockup/" /tmp/gn-preview/02_work/mockup/
```

### ユーザーへの出力ルール（毎回必須）

HTMLファイルを作成・更新したら、応答に必ず以下を明記する:

```
📂 保存先（Google Drive）:
  共有ドライブ/01_顧客・プロジェクト/g_株式会社G＆N/01_LP制作_202604/{相対パス}

🌐 プレビューURL:
  http://localhost:3333/{相対パス（URLエンコード済み）}
```

- 複数ファイルを同時に出力した場合は、ファイルごとに1行ずつ列挙する
- プレビューサーバーが起動していない場合は `preview_start` で起動してから案内する
- 日本語ファイル名はURLエンコードして記載する

---

## §8 スキル / プラグインの活用方針
利用可能なスキルは、用途に合う局面で**能動的に呼び出して活用**する。

| スキル | 活用タイミング |
| --- | --- |
| `frontend-design` | HTMLモックアップ生成時（デザイン壁打ち③）・Studio実装指示書のビジュアル指定時 |
| `natural-japanese-writing` | LPコピー・見出し・CTA文言等のライティング時 |
| `pptx` / `docx` / `xlsx` / `pdf` | インプット資料の読み込み・成果物の文書化が必要になった時 |
| `media-pipeline`（Gemini画像生成） | LP用ビジュアル素材の生成（FVヒーロー画像・課題提示イラスト等）。詳細は §12 |

- 壁打ちで具体的なビジュアル検討に入る前には、**必ず `frontend-design` スキルを起動してから**作業する
- コピー確定の工程では `natural-japanese-writing` を併用する
- 新規スキル/プラグインが追加された場合、本表を更新する

## §12 Gemini画像生成ワークフロー（2026-04-27 追加）

LP用のビジュアル素材を Gemini 画像生成 API で作成し、HTMLモックアップに組み込む運用。

> 🔗 **新規セッションのセットアップは [`setup_gemini.md`](setup_gemini.md) を参照**（初心者向け、5ステップ・約30分で完了）

### 12.1 利用する仕組み（2系統併用）

本プロジェクトでは画像の用途・品質要件に応じて **2系統の画像生成ルート**を使い分ける:

| ルート | 仕組み | 用途 | コスト |
| --- | --- | --- | --- |
| **A. media-pipeline プラグイン** | MCP（claude-image-gen / Gemini） | アイコン・装飾・量産系の軽量画像 | 低 |
| **B. `imagen.sh` ヘルパースクリプト** | Bash + Imagen 4 直接API | **FVヒーロー・課題提示イラスト等の高品質画像** | 中〜高 |

#### A. media-pipeline プラグイン（既存）
- **プラグイン**: [`claude-image-gen`](https://github.com/guinacio/claude-image-gen)（marketplace: `guinacio/claude-image-gen`）
- **インストール済みプラグイン名**: `media-pipeline@media-pipeline-marketplace`
- **インストール経路**: Claude Code CLI 経由（`claude plugin install`）。`~/.claude/plugins/` 共有でデスクトップアプリも自動認識
- **モデル**: `gemini-2.5-flash-image` / `gemini-3.1-flash-image-preview` / `gemini-3-pro-image-preview`
- **呼び出し**: Claude が `mcp__plugin_media-pipeline_media-pipeline__create_asset` ツールで実行

#### B. `imagen.sh` ヘルパースクリプト（新規・高品質ルート）
- **配置先**: `02_work/scripts/imagen.sh`（実行権限付き）
- **対応モデル**: Imagen 4 / Imagen 4 Ultra / Imagen 4 Fast / Imagen 3
  - 既定: `imagen-4.0-generate-001`（標準・高品質）
  - 最高品質: `imagen-4.0-ultra-generate-001`
  - 高速: `imagen-4.0-fast-generate-001`
- **アスペクト比**: 1:1 / 16:9 / 9:16 / 4:3 / 3:4 / 3:2 / 2:3
- **呼び出し**: Claude が Bash 経由で実行
  ```bash
  cd 02_work && ./scripts/imagen.sh "プロンプト" 16:9 mockup/assets/generated/{命名規則}.png [モデル]
  ```
- **メリット**: プラグイン非依存・Imagen 4 の純画像生成品質を活用可
- **両セッション共通**: スクリプトは Git 共有・追加インストール不要

#### 共通要件
- **API キー**: `GEMINI_API_KEY` 環境変数（macOS `launchctl setenv`）
- **Billing**: Google Cloud で有効化必須（無料枠なし）
- **依存**: jq（macOS 標準）, curl（macOS 標準）

### 12.1.1 使い分け基準

| 画像の用途 | 推奨ルート | モデル |
| --- | --- | --- |
| **FV ヒーロー画像** | B (`imagen.sh`) | `imagen-4.0-generate-001` または Ultra |
| **課題提示イラスト** | B (`imagen.sh`) | `imagen-4.0-generate-001` |
| **背景装飾・blob** | B (`imagen.sh`) | `imagen-4.0-generate-001` |
| **小アイコン・サムネ** | A (media-pipeline) | `gemini-2.5-flash-image` |
| **量産・テスト** | A (media-pipeline) | `gemini-2.5-flash-image` |
| **試作・iter数多め** | A (media-pipeline) | `gemini-2.5-flash-image`（コスト抑制） |

### 12.2 アセット格納先
```
02_work/mockup/assets/
├── INDEX.md           ← 生成履歴・採用状況の管理
├── generated/         ← 生成画像本体（Gemini出力）
└── （将来）vc_logos/, case_logos/, originals/ 等
```

### 12.3 ファイル命名規則
`{section}_{purpose}_{description}_{seq}.{ext}`

- **section**: `fv` / `problem` / `solution` / `service` / `record` / `vc` / `case` / `cta` / `common`
- **purpose**: `hero`（メインビジュアル）/ `bg`（背景）/ `icon` / `illust`（イラスト）/ `card`（カード装飾）
- **description**: 内容の英語/ローマ字短縮
- **seq**: 同テーマの連番（`01`, `02`...）

例: `fv_hero_startup-tailwind_01.png`, `problem_illust_4-pain-points_01.png`

### 12.4 アスペクト比指定
| 比率 | 主用途 |
| --- | --- |
| `16:9` | FVヒーロー、横長バナー |
| `1:1` | カード装飾、アイコン枠 |
| `9:16` | モバイルストーリー |
| `4:3` | 一般的な記事内画像 |
| `3:2` | 写真風画像 |

### 12.5 生成・運用フロー（標準手順）
1. **生成前**: `design_rules.md` のトンマナを確認し、生成プロンプトに反映
2. **プロンプト作成**: スタイル指定（イラスト/写真風/フラット等）・色味（爽やか系・ライトブルー等）・避ける要素（ストックフォト感等）を含める
3. **生成実行**: `media-pipeline` スキル経由で Gemini を呼び出し
4. **保存先**: `02_work/mockup/assets/generated/{命名規則準拠ファイル名}`
5. **生成記録**: `assets/INDEX.md` の「生成履歴」テーブルに追記（日付・ファイル名・プロンプト要約・用途）
6. **モックアップ組み込み**: HTML側で `<img src="assets/generated/{ファイル}.png">` で参照
7. **採用時**: `assets/INDEX.md` の「採用済み画像」テーブルに追記
8. **ログ記録**: 生成・組み込みの前後で `activity_log.md` に `[INTENT]` / `[DONE]` 記録

### 12.6 トンマナ準拠ガイド（プロンプト作成の指針）
画像生成プロンプトには **必ず以下を含める**:
- ✅ ライトブルー・グリーン系の爽やかな色味（hubble系を採用するなら濃紺×ブルー×イエロー）
- ✅ モダン・親しみやすい・スタートアップ向け
- ✅ フラットイラスト or アイソメトリック調（写真は使用しない方針のため）
- ❌ ストックフォト感・人物写真・ベタなグラデーション
- ❌ 黒×白×青のシック路線（コーポレートサイトと差別化）

### 12.7 2セッション運用
- **API キー**: 同じキーを両セッションの環境変数として共有可。コスト管理は `assets/INDEX.md` で生成回数を可視化
- **担当割当**: 同じ画像を両セッションが重複生成しないよう、生成前に `activity_log.md` `[INTENT]` で対象を宣言
- **採用判断**: 画像の最終採用はユーザー判断。両セッションが提案 → ユーザーが選定する運用

### 12.8 コスト・注意事項
- Gemini 画像生成は**従量課金**（Google AI Studio で確認）
- 生成回数は `assets/INDEX.md` で集計可能。LP1本制作で過剰になっていないか定期確認
- 生成済み画像は**消さずに残す**（採用しなかった案も後から比較可能に）

### やらないこと
- ユーザーの明示指示なく `01_input/` を変更する
- Studioの実装作業そのもの（AgentはStudio操作権限を持たない前提）
- スコープ外の提案を独自判断で実装に進める

## 6. 未確定事項 / 今後の確認ポイント

**詳細は [questions.md](questions.md) を参照。** ここには本ファイルに関わる項目のみ残す。

- [x] 作業ディレクトリ名: `02_work` で確定（2026-04-23）
- [x] 制作ワークフロー: HTMLモックアップ壁打ち＋Studio実装指示書方式で確定（2026-04-24）
- [x] Studio納品方式: 別ドメイン・ユーザー手動実装で確定（2026-04-24）
- [x] LPの目的: 紹介・説明ツール型。スタートアップCEO/CFO向け（2026-04-24）
- [x] ターゲットユーザー像: シード〜シリーズA スタートアップ CEO/CFO、1期以上・1名以上雇用（2026-04-24）
- [x] CTA: 「1時間で最適な制度を提案します」／フォーム3項目のみ（2026-04-24）
- [ ] Studio上のプロジェクト/テンプレートの有無（既存 or 新規）
- [ ] ブランドガイドラインの具体HEX値 → questions.md Q8
- [ ] 公開スケジュールの実態 → questions.md Q4

---

## §14 Studio 仕様の管理ルール（2026-05-07 確定）

### 14.1 一次資料の単一化

- Studio.design の仕様（UI 配置・機能・制約・プラン制限・要素挙動 等）は **`02_work/studio_guide/studio_spec.md`** を唯一の一次資料とする
- 他の文書（Studio 実装指示書 `studio_guide/drafts/v01_*.md` 等）は本仕様書を**参照して作成・修正**する
- 本仕様書と指示書に齟齬がある場合は **本仕様書が正**

### 14.2 仕様確認・反映の基本フロー

ユーザーとのやり取りで Studio の実 UI / 挙動が判明した時の処理:

```
1. ユーザーが画面共有・スクリーンショット・実機検証で事実を提示
   ↓
2. Agent は「studio_spec.md に既出か」を確認
   ↓
3-A. 既出だが不正確 → spec.md を訂正（更新履歴に追記）
3-B. 未記載 → spec.md に追加（出典・観測条件を明記）
   ↓
4. spec.md の更新後、関連する指示書（v01_*.md 等）を spec.md に合わせて更新
   ↓
5. 更新内容を activity_log.md に [DONE] で記録
```

### 14.3 Agent の振る舞いルール

- ❌ **Studio の仕様について推測で回答しない**。spec.md にない事実は「未検証」と明記する
- ✅ **公式ドキュメント or 実機検証**を根拠に回答する
- ✅ 公式ドキュメント参照時は **URL を spec.md と回答に併記**する
- ✅ ユーザーから新しい事実が判明したら **即 spec.md に反映**する（後回しにしない）
- ✅ 指示書を作成・修正する時は **spec.md の該当節への参照**を含める

### 14.4 不確実性の表記ルール

spec.md および指示書では以下の凡例を使う:

| 記号 | 意味 |
| --- | --- |
| ✅ | 公式情報・実装で検証済 |
| 📚 | 公式ドキュメントに明記 |
| 🟡 | 一般的に知られているが公式記述未確認 |
| ⚠️ | 未検証・要調査 |
| 🔬 | 本プロジェクトの実装で検証 |

推測を断定的に書かないこと。

### 14.5 spec.md と指示書の整合性チェック

定期的に（または大幅な変更時に）以下を実行:

- spec.md の項目で指示書に反映されていないものがないか
- 指示書の記述が spec.md と矛盾していないか
- 古い情報・廃止された機能が指示書に残っていないか

矛盾を発見したら spec.md を正として指示書を修正する。
