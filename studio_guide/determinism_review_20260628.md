# 決定性レビュー（誰がやっても同じ結果か）2026-06-28 azalea

基準: 作業手順書として「誰がやっても同じ結果／作業者に判断させない」。全23章で2人で結果が変わりうる箇所を抽出。

## サマリ

総findings **399**（高43 / 中189 / 低167）。

### カテゴリ別

- 選択肢未確定: 102
- 検証/実機裁量: 91
- 曖昧値: 62
- 任意・省略可: 54
- 推奨止まり: 46
- 判断語: 44

### 章別

| 章 | 判定 | 件数 | 高 |
|---|---|---|---|
| g0 0. レスポンシブ・モバイル前提（最 | 一部に判断あり | 15 | 2 |
| g1 1. Studioプロジェクト初期設 | 一部に判断あり | 14 | 0 |
| g2 2. デザイントークン（全体共通） | 一部に判断あり | 12 | 1 |
| g3 3. グローバルレイアウトと z-i | 一部に判断あり | 13 | 0 |
| g4 4. 共通コンポーネント | 一部に判断あり | 19 | 0 |
| s1 5-1. ヘッダー | 一部に判断あり | 15 | 5 |
| s2 5-2. FV（Hero） | 一部に判断あり | 19 | 1 |
| s3 5-3. アプローチ（Approac | 一部に判断あり | 10 | 1 |
| s4 5-4. 採択率 / 市場動向 | 判断・曖昧が多い | 19 | 7 |
| s5 5-5. サービス（Service） | 一部に判断あり | 22 | 3 |
| s6 5-6. 実績（Record） | 一部に判断あり | 14 | 3 |
| s7 5-7. VC提携（VC Partn | 一部に判断あり | 13 | 2 |
| s8 5-8. 導入事例（Cases） | 一部に判断あり | 13 | 1 |
| s9 5-9. FAQ | 一部に判断あり | 12 | 0 |
| s10 5-10. CTA | 一部に判断あり | 23 | 0 |
| s11 5-11. フッター | 一部に判断あり | 11 | 4 |
| s12 5-12. フローティング特典CTA | 判断・曖昧が多い | 15 | 1 |
| t6 6. アニメーション一覧と実装方針 | 一部に判断あり | 24 | 0 |
| t7 7. レスポンシブ仕様（セクション別 | 判断・曖昧が多い | 23 | 5 |
| t8 8. アセット一覧 | 判断・曖昧が多い | 19 | 1 |
| t9 9. Embed / Loop Bo | 一部に判断あり | 17 | 2 |
| t10 10. 公開前 QA チェックリスト | 一部に判断あり | 29 | 3 |
| t11 11. モバイル実装の落とし穴（v1 | 判断・曖昧が多い | 28 | 1 |

## 全findings（severity順）

### severity 高

- **[g0|選択肢未確定]** §0.2 ブレークポイント定義表 Small 行 / §0.2 操作 step2
  - 問題: 1024px（有効化して設定。1024 不可なら既定 1140 でも可）／1024 が弾かれて戻る環境のみ既定 1140 のままでも可（その場合も各章の Small 指定値はそのまま使う）
  - 修正: 「Small 基準幅は 1024px を入力する。数値が弾かれる場合は1024px までガイド線をドラッグして確定し、それでも不可なら作業を止めて管理者に確認」と単一手順に固定する（1140 容認を削除）。
- **[g0|選択肢未確定]** §0.3 末尾 note【補足】スモールを使わず3段で済ませる簡易案
  - 問題: スモールを増やしたくない場合は Tablet を上限の 990px に設定し、1024 の1カラム化を Tablet(990)/Mobile(540) に置く方法もある…（許容だが非推奨）
  - 修正: 「本手順では必ず Small(1024) を有効化する4段構成を採る。Tablet=990 の3段簡易案は使用しない」と言い切り、簡易案の記述は参考扱いから削除または『採用しない』と明記する。
- **[g2|曖昧値]** §2.4 コンテナ/セクション表「コンテナ左右パディング」行
  - 問題: SP <code>16px</code>（さらに狭で 14/10px）
  - 修正: Studio 3BPに合わせ「Tablet 16px / Mobile 16px」と単一値に固定し、14/10px の記述は削除（特定幅で別値が要るなら『◯px 未満は14px、◯px 未満は10px』とBP値を明記）。
- **[s1|検証/実機裁量]** 5-1-7 step1（確認ステップ）
  - 問題: ユーザー/デザイナーに「スクロール時にヘッダー下境界線（line = rgba(15,26,51,.08)）を出すのがデザイン意図か、それとも境界線を出す指定が無いことによる取りこぼしか」を確認する。
  - 修正: 本書時点の正として「下境界線は透明1px固定（scrolled状態は作らない）」と1つに確定し、確認は別途の課題管理に切り出す。
- **[s1|選択肢未確定]** 5-1-8 写像注（768–840px帯）
  - 問題: この帯域差は許容するか、768px に近い切替が必須なら Tablet 基準幅をツールバー数値入力で 767 近辺へ寄せる（§8.1）。
  - 修正: 「768–840px帯の差異は許容し、Tablet基準幅は変更しない（840のまま）」と1つに確定して言い切る。
- **[s1|検証/実機裁量]** 5-1-8 写像注（Tablet基準幅変更の連動）
  - 問題: Tablet 基準幅を動かすと他セクションの Tablet 写像も連動するため、変更する場合は全章横断で要確定（運用方針として要決定）。
  - 修正: 「Tablet基準幅は840から変更しない」と確定し、本注記を変更不可の宣言に置き換える。
- **[s1|選択肢未確定]** 5-1-8 写像注（380px↔540px値差の扱い）
  - 問題: Mobile では<strong>より小さい側（380px相当値）に寄せる</strong>か、<strong>Mini(320)BP を追加</strong>して2段に分けるかを運用方針として要確定。
  - 修正: 「Mobileに380px相当値（padding10・CTA 7/26/7/12・11px）を統合し、Mini(320)BPは追加しない」と1つに確定する。
- **[s1|選択肢未確定]** 5-1-8 Mobile step3（ハンバーガー）
  - 問題: Studio で追加する場合は Toggle（…）で button（≡アイコン）＋ content（ナビ項目の縦並び）を作り、Tablet/Mobile のみ表示。
  - 修正: 「Free制作中はハンバーガーを追加せず、Tablet/Mobileはロゴ＋CTAのみ・ナビ非表示で確定」と言い切る。
- **[s2|選択肢未確定]** 5-2-9 Mobile 案1/案2 + 5-2-11（line 724-727, 761）
  - 問題: 対応は2案（採否は 5-2-11 でユーザー承認）。
  - 修正: 本書として案1か案2のどちらを正とするか1つに明記する（承認待ちなら「承認まで案1で実装」等のデフォルトを断定）。
- **[s3|選択肢未確定]** 5-3-7 方針(A)/(B) の選択
  - 問題: 下記いずれかを既定手順として必ず適用します。… 本章は (A) を既定（推奨） とし
  - 修正: 「(A)を採用する。(B)は使用しない」と一意に確定し、Tablet帯は2カラム維持と言い切る
- **[s4|検証/実機裁量]** 冒頭 callout warn（5-4導入・必読）
  - 問題: 本章では「Box fill のグラデは可否未検証（要実機確認）」として扱い、グラデを使う各箇所に【ネイティブが使えた場合】と【代替（未確定時の正規手順）】の両方を必ず併記します。グラデを既定の推奨として断定しません。
  - 修正: 本書時点での確定結論を1つに固定する（例:「Box fillの線形グラデは使用不可と確定。背景はCustom Code、バーは単色終点色を必ず採用」）。実機確認に委ねず単一手順に断定する。
- **[s4|選択肢未確定]** 5-4-2 ステップ5 / 背景グラデ実装の分岐（plan free / plan mini / callout note）
  - 問題: 背景グラデの実装は以下のいずれか。【ネイティブが使えた場合（要実機確認）】… 3点以上を置けない場合は下記Mini代替へ。【代替・未確定時の正規手順 / Mini以上】… Custom Code …【代替・Free段階の暫定 / Custom Code不可時】… 単色 white
  - 修正: プランは別途固定済みのはずなので、その固定プラン前提で背景実装を1つに確定（例:「背景はCustom Codeブロックを貼付して実装する」一手順のみ記載）。他選択肢は削除。
- **[s4|選択肢未確定]** 5-4-5 バー塗り分岐（plan free recommended / callout note / callout tip）
  - 問題: 【ネイティブが使えた場合（要実機確認）】… 実機で2色線形グラデが入力できるか確認してから採用。【代替・未確定時の正規手順 / Free可】グラデ不可なら 単色（最も濃い終点色）で代替 …【代替・2Box重ね / Free可】よりグラデの見た目に寄せたい場合は …
  - 修正: 「左カードバーは単色（prev=navy / now=bar-gray-2）で実装する」等、単一手段に確定し他案を削除。
- **[s4|選択肢未確定]** 5-4-6 high バー塗り分岐（plan free recommended / callout note）
  - 問題: 【ネイティブが使えた場合（要実機確認）】線形180deg 起点 = yellow / 終点 = yellow-dk。【代替・未確定時 / Free可】単色 yellow（#FFD166）または yellow-dk（#E8B84A）の塗りで代替
  - 修正: 単一に確定（例:「up-bar-highは単色yellow #FFD166で実装」）し他案を削除。
- **[s4|検証/実機裁量]** 5-4-8 ネイティブ可否まとめ 表（背景グラデ行）
  - 問題: 実機で4ストップ線形グラデが入力可なら採用。不可なら Custom Code（Mini以上）… Free暫定は単色white＋未実装を指示書明記
  - 修正: 確定プラン前提の単一採用方法を表に記載（例:「Custom Codeで実装」のみ）。
- **[s4|検証/実機裁量]** 5-4-8 ネイティブ可否まとめ 表（バーグラデ行）
  - 問題: 実機で2色線形グラデが入力可なら採用。不可なら 単色（濃い終点色 navy / bar-gray-2 / yellow）。2Box重ねは…次善
  - 修正: 「全バー単色（prev=navy/now=bar-gray-2/low=bar-gray-2/high=yellow）で実装」と確定記載。
- **[s4|検証/実機裁量]** 5-4-8 末尾 callout warn
  - 問題: 発注前に実機で「Box fill が線形グラデ（特に多ストップ）に対応するか」を確認し、不可の場合は背景＝Custom Code（Mini以上）、バー＝単色代替を正規手順として採用してください。
  - 修正: 本書で確認済みの単一結論を前提に正規手順を1本化し、「要実機確認」を撤去。
- **[s5|曖昧値]** 5-5-3 子カード幅 Free代替（Flex折返し）
  - 問題: %指定する場合は 48%前後＋gap20pxで2列に収まるよう調整。
  - 修正: 「子カード幅= calc(50% − 10px) を入力する（%直書きはしない）」と単一手順に確定する。
- **[s5|曖昧値]** 5-5-4 カード外枠 サイズ
  - 問題: サイズ: 幅 約48%（2列配分）、高さ auto。
  - 修正: 「幅= calc(50% − 10px)」と確定値で指定する（5-5-3と統一）。
- **[s5|選択肢未確定]** 5-5-6 点線コネクタ 方式選択
  - 問題: 装飾目的なので扱いは下記から1つ選びます。
  - 修正: 「Box2枚で十字を組む方式（推奨）で実装する」と1案に確定し、他案は参考情報として削除/封印する。
- **[s6|選択肢未確定]** 5-6-7 写像レンジのズレ / Tablet 手順2
  - 問題: 767–691帯の2×2が必須要件なら、Mobile 節の Rec Grid/Rec Stat 設定を Tablet にも複写する
  - 修正: 本書として「767–691帯は1行4ブロックのまま許容する（複写しない）」または「複写する」を断定し、条件分岐文を削除する
- **[s6|検証/実機裁量]** 5-6-4 手順5 Free代替 / 5-6-6 手順2 Free代替
  - 問題: 左右クロップの見え方が完成イメージと一致するかは公開URLで実機確認（px レベルで一致しない場合あり＝confidence: 未検証）
  - 修正: プランを1つに固定（例: Mini以上のCustom Code主案で確定）し、background-size:auto 100%＋background-position:left/right center を唯一の確定手順として実機裁量を排除する
- **[s6|推奨止まり]** 5-6-4 手順5 主案 / 5-6-6 手順2 主案
  - 問題: 主案・推奨：Custom Code（body）でCSS上書き … 確実性を優先し Mini 以上で Laurel L/R Box に次のCSSを当てるのを主案・推奨とします
  - 修正: プラン方針（Free/Mini）を先に1つに固定し、それに対応する単一手順だけを残して「推奨」を「これにする」と断定する
- **[s7|選択肢未確定]** 5-7-2 C / 「50社以上」黄色マーカー下線（Free代替 vs Mini）＋直後 tip
  - 問題: （推奨）1行目を3つの Text に分割して横並びにする。 … 推奨は Mini（Custom Code）。 Free代替のBox分割は…
  - 修正: 本LPはFree方針と他箇所に明記があるので「50社以上の黄色マーカー下線は Free代替（vc-title-mark Box分割・180deg黄帯62-90%・padding6）で作る。Miniは使わない」と一意に確定し、tipの「推奨は Mini」表現を削除する。
- **[s7|選択肢未確定]** 5-7-3 B / 巨大数字 vc-big（Free代替）
  - 問題: (a) 画像化（推奨）… (b) 単色代替：Text「50」+ 上付き「+」を #4A7DE8（blue）単色…で置く。グラデは諦める。
  - 修正: 「vc-big は (a) 画像化で作る（blue→navy 135degグラデ＋装飾円を1枚のPNG/SVGに描き込み、高さ180px）。(b)単色代替は使わない」と1案に確定する。
- **[s8|推奨止まり]** 5-8-2 見出しコピー（li 改行の項）
  - 問題: 全BPで改行ありのまま運用するか、改行なし1本に統一する。【推奨】改行なし1本（スタートアップの成長を支えた補助金活用の実例）に統一し
  - 修正: 「改行なし1本（スタートアップの成長を支えた補助金活用の実例）にする」と断定し、改行運用の選択肢を削除する。
- **[s11|選択肢未確定]** §2 上段グリッド（ft-inner）冒頭・(A)(B)2方式の提示
  - 問題: 推奨は (A) 横並び flex 再現です … (B) 簡易再現: 横並び Box にし、ft-info を左寄せ＋幅を最大幅で左カラム相当に制限する。
  - 修正: 本書は (A) 横並び flex（左1.3flex／右空Box2flex／gap60）を正とする、と1方式に断定し (B) は削除または「採用しない」と明記。
- **[s11|推奨止まり]** §2 (A)推奨ブロック『推奨はX』
  - 問題: 推奨は (A) 横並び flex 再現です（flex 比＝1.3:2 比で…）
  - 修正: 「(A)を採用する（必須）。(B)は使用しない」と確定表現にする。
- **[s11|判断語]** §9 レスポンシブ・写像のズレ帯対応
  - 問題: 対応は次のいずれかを作業者判断で選ぶこと
  - 修正: プロジェクト方針を1つに確定（例「(b)既知差分として近似BP 840/540のまま進める」）と断定し、もう一方を削除。
- **[s11|選択肢未確定]** §9 ズレ帯対応 (a)/(b) の二者
  - 問題: (a) フィデリティ優先 … (b) 許容する場合
  - 修正: 本書として(a)(b)いずれかを正と断定し、選ばれない側は手順から除外。
- **[s12|選択肢未確定]** §5-12-11 Tablet 実装（行637-657）
  - 問題: Tablet を【正確再現】=手順1-9上書き／【簡略】=PC継承 のどちらで実装したか確定
  - 修正: 本書として「Tablet にも手順1-9の全幅化を上書きする（正確再現）」に一本化し、簡略案は削除する（推奨と明記済みなので断定に格上げ）
- **[t7|判断語]** §7.0 写像表 380px 行 / §7.5 表 380行 / §7.9 表 380行
  - 問題: 380 は必要時のみ Mini を追加 …h2 等の極小値が必要な箇所のみ Mini(320px) を追加して当てる（各表で指定）
  - 修正: Mini(320) を追加する対象要素と条件を確定列挙する（例:「FV の h1 と CTA の h2 は必ず Mini(320) を追加し、それ以外は追加しない」）
- **[t7|検証/実機裁量]** §7.1 手順3
  - 問題: 以下のいずれかが実機確認で起きた場合のみ追加する。…窮屈に崩れる → Small(1140) を追加
  - 修正: 追加要否を実機判断に委ねず「Small・Mini は追加しない（または常に追加し下記値を当てる）」と一律に確定する
- **[t7|選択肢未確定]** §7.3 Free代替/Mini 並記 / §7.4.3 / §7.5.2 / §7.15.1
  - 問題: 【Free 代替】…【Mini】… の二択が各所に併記（例: Safe Area を Free=マージン増 / Mini=env() 注入）
  - 修正: 本 LP の採用プランを冒頭で1つに固定し（例: Mini を正とする）、各 Free/Mini 併記を採用プラン側に一本化する
- **[t7|曖昧値]** §7.5.1 手順3 / §7.5 表 h1 / §7.6 §7.7 §7.13 各 section-title
  - 問題: サイズ clamp(32px,5.4vw,52px) …（clamp(…) は本章記載の固定px をその BP に入れればOK（連続スケールは再現不可）
  - 修正: 各 clamp について BP ごとに入力する単一 px を明記する（例: Tablet の h1 は 44px、Mobile は 30px と確定）
- **[t7|推奨止まり]** §7.15.1 実装方式の選択
  - 問題: Embed 実装を最優先推奨 … Embed（推奨）/ Free代替 の二案併記
  - 修正: 本 LP は「Embed で実装する」と言い切り、§7.15.2 は Embed を選ばない場合のみという条件を消して採用方式を1つに固定
- **[t8|選択肢未確定]** §8.1 / §8.9-4 月桂冠 B案
  - 問題: B 案（Free 代替・要検証）：1 ファイルを Box 背景にして position 指定 … 実機確認できた場合のみ採用。再現できなければ A 案へ
  - 修正: 「本書は A 案（左右2ファイル分割）を正とする。B案は採用しない」と1案に確定する
- **[t9|選択肢未確定]** §9.2 Free代替
  - 問題: ①ラフネスを諦めてクリーンな下線にする…②ラフなマーカーストロークを透過 PNG/SVG 画像として焼き込み…見出し語の背面に Image 配置。
  - 修正: 「本書は①を正とする（Mini切替後に§9.3で再現）」のように1案へ確定する。
- **[t9|選択肢未確定]** §9.3 Free代替（推奨 vs 妥協案）
  - 問題: <strong>妥協案</strong>：見出しを通常 Text にし、強調語のみカラースタイル blue 単色＋背面に静的な黄ハイライト Box（skew なし）。
  - 修正: 「本書は Embed sandbox を正とする。妥協案は採用しない」と確定する（または採用条件を一意に定義）。
- **[t10|選択肢未確定]** 10.3 表示確認 Mobile
  - 問題: sec-header がハンバーガー化 or Nav 非表示＋CTA のみになっている
  - 修正: Mobile ヘッダーは「ハンバーガー化」に確定（実装方式と展開Toggle仕様まで指定）し、もう一方は不合格と明記する。
- **[t10|選択肢未確定]** 10.6 フォーム 送信後挙動
  - 問題: 送信後の遷移先を 別ページ（サンクスページ）を作成して post-submission destination に指定、または成功メッセージを日本語に設定
  - 修正: 「サンクスページを別途作成し post-submission destination に指定する」方式に確定し、成功メッセージ案は不採用と明記する。
- **[t10|選択肢未確定]** 10.8 法務
  - 問題: 🔴 実績 lead の「交付決定率 100%・意図しない返還事故ゼロ」（legal §1.1）: 根拠資料確認 or 注釈追加 or 表現変更を反映済み
  - 修正: 採用する対応（例: 表現変更後の確定文言）を1つに決めて記載し、他の選択肢を排除する。
- **[t11|判断語]** 11.0 補足／チェックリスト
  - 問題: 480px/380px 対応が必要か判断し、必要なら Mini(320) を追加
  - 修正: 「Mini(320)を追加する／追加しない」を本書として一方に確定（例: Mini(320)を必ず追加し380で h1=24px を適用）

### severity 中

- **[g0|曖昧値]** §0.3 改行出し分け note【方針】2点目
  - 問題: 多用は避け、FV 見出しなど効果の大きい1–2箇所に限定する
  - 修正: 2重化する対象を具体的に列挙（例「FV見出し（hero-title）のみ PC用/Mobile用 Text を2レイヤー用意し、それ以外は2重化しない」）と1つに確定する。
- **[g0|推奨止まり]** §0.5 両対応の原則 / §0.5 末尾【推奨】callout
  - 問題: 各要素の手順末尾に必ず次の2案を併記し、推奨を1つ明示する／原則として制作中は Free 代替で破綻なく組み…方針を既定とする
  - 修正: 「制作中は必ず Free 代替で実装する。Mini の Custom Code は納品後のMini切替時にのみ、指定された装飾に限り適用する」と命令形で固定する。
- **[g0|検証/実機裁量]** §0.4 早見表 グラデーション塗り dd
  - 問題: 線形⇔放射の切替・停止点の%数値入力・停止点ごとの不透明度スライダーは公式未確認＝実機で要確認。確実にしたい時は Mini の Custom Code に…直接記述
  - 修正: グラデーションは「Mini切替後に Custom Code で linear/radial-gradient を直接記述する」を唯一の手順と定め、ネイティブUIでの作成を選択肢から外す。
- **[g0|曖昧値]** §0.4 早見表 フィルター（ぼかし）dd / 実装後 ヘッダー背景ぼかし
  - 問題: ぼかし量の入力UIは公式未確認・Firefox は背景ぼかし（backdrop-filter）非対応なので不透明寄りの背景色をフォールバックに
  - 修正: フォールバック背景色を確定HEX/RGBAで明記（例「フォールバックは white 92% = rgba(255,255,255,0.92)」など単一値）にする。
- **[g0|選択肢未確定]** §0.5 レスポンシブ固有の注意 3点目
  - 問題: フォーム送信後のサンクス遷移を別ページにするか同一ページ内表示にするかで必要ページ数が変わるため…法務確認で確定する
  - 修正: 本章では「サンクスは同一ページ内表示とする（または別ページとする）」のいずれかに仮確定するか、確定済みの結論を参照リンクで指定する。
- **[g0|曖昧値]** §0.7 テキストスタイル例 callout
  - 問題: 例: H2 Section = Noto Sans JP 900 / PC clamp(28px,4vw+8px,52px) lh1.22 / Mobile clamp(24px,6.2vw,32px) lh
  - 修正: Mobile の line-height を具体数値で明記（例 lh1.3）して欠落を埋める。
- **[g0|検証/実機裁量]** §0.5 レスポンシブ固有の注意 2点目
  - 問題: 可否未確定のネイティブ機能（On Appear / Hover / Scroll Effect の Free 可否は…未検証）に依存する設計は避け…Mini 切替後の有効化前提として各章に明記する
  - 修正: 「On Appear / Hover / Scroll Effect は制作中(Free)では使用せず、必ず Mini 切替後に有効化する」と一律に確定し、未検証ゆえの個別判断を排除する。
- **[g1|選択肢未確定]** 前提プラン callout（line7）
  - 問題: 制作中は Free（既存 orange265484.studio.site を流用、または新規 *.studio.site）
  - 修正: 「既存 orange265484.studio.site を流用する（新規は作成しない）」のように1つに確定して言い切る。
- **[g1|判断語]** 1.2 step3（line158）
  - 問題: 複雑な中間崩れが Tablet 1段で吸収しきれない箇所が出たら、その章で個別に Small(1140) 追加を検討する
  - 修正: 本章では「Small/Mini は追加しない」を確定とし、追加要否の判断は当該セクション章の確定基準に委ねると明記（本章成果物からは除外）。
- **[g2|曖昧値]** §2.4 gap/padding 代表表「見出し下の要素マージン」行
  - 問題: <code>24–28px</code>
  - 修正: 単一値に確定（例『見出し下マージンは一律 28px』）。要素別に異なるなら対象クラスごとに 24px / 28px を割り当てて列挙する。
- **[g2|曖昧値]** §2.4 gap/padding 代表表「最頻の標準 gap」行
  - 問題: <strong><code>10px</code></strong>、次いで <code>14px</code>（カード間に多用）
  - 修正: 本表は「正本」とされるので、各要素の gap を確定するのは本表ではなく各セクション章である旨を明記し、ここは『代表ステップの参考値（実値は各章で指定）』と位置づけを断言する。
- **[g2|曖昧値]** §2.4 gap/padding 代表表「カードグリッド gap」行
  - 問題: <code>22px</code>（prob/appr/mkt/svc）。中間に 16 / 24px
  - 修正: グリッド種別ごとに 16/22/24px を割り付けて列挙するか、本章では 22px に統一し例外は各章で指定と断言する。
- **[g2|曖昧値]** §2.3 レスポンシブ表「Footnote」Mobile 列
  - 問題: <code>10–11px</code>
  - 修正: 単一値に確定（例『Mobile は 11px』）。
- **[g2|推奨止まり]** §2.2 副フォント運用の callout warn
  - 問題: 用途別に別ファミリー（Noto Sans JP / Inter を別エントリ）</strong> を保持し、要素ごとにフォントを使い分ける運用を推奨。
  - 修正: 「本LPは副フォントを使わず Noto Sans JP / Inter を別ファミリーで保持し要素ごとに使い分ける（副フォント統一は使用しない）」と断言する。
- **[g2|検証/実機裁量]** §2.6 シャドウ制約 callout warn / 完了チェック
  - 問題: 複数レイヤーの影（2 層指定）は、Studio で影を複数追加できる範囲で重ねる。
  - 修正: 本章はプラン方針（Free/Mini）で1層近似 or Custom Code 注入が確定済みなので、『2層が組めない場合は必ず◯◯（濃い側1層）に固定』と単一の手順へ寄せて『範囲で重ねる』表現を削除。
- **[g3|選択肢未確定]** §3.3 Free代替（2層 box-shadow）
  - 問題: 影 Box を 2 枚重ねる、または主要な 1 層 0 18px 44px rgba(15,26,51,.22) のみ採用して簡略化（視覚差は軽微）。推奨: 1 層に簡略化
  - 修正: 「Free では1層 0 18px 44px rgba(15,26,51,.22) に確定」と単一手順で言い切る（2枚重ね案を削除）
- **[g3|推奨止まり]** §3.4 Free代替（Hero ブロブ）
  - 問題: Image 化（…）を推奨（…）。Box ＋角丸近似でも可だが有機的な形は再現性が落ちる
  - 修正: 「Hero ブロブは必ず Image 化（SVG書き出し）で配置する。Box＋角丸近似は使用しない」と言い切る
- **[g3|任意・省略可]** §3.5 写像の判断基準
  - 問題: 380px の極小調整は Mobile 内で吸収（必要なら任意で Mini=320 を追加可）
  - 修正: 「Mini=320 は追加しない（Mobile 内で吸収する）」と固定する
- **[g4|曖昧値]** §4.0 テキストスタイル表 English Label行
  - 問題: <code>11–12px</code> ... <code>700–800</code> ... <code>.12–.15em</code>・UPPERCASE
  - 修正: 単一値に確定する（例: size 12px / weight 800 / letter-spacing .14em）。
- **[g4|曖昧値]** §4.0 テキストスタイル表 H3 Card行
  - 問題: <code>20–22px</code> / <code>17px</code>
  - 修正: PC値を単一に確定する（例: 22px）。
- **[g4|検証/実機裁量]** §4.0 clamp入力tip
  - 問題: 流体カーブの厳密一致は Live Preview で確認。
  - 修正: clamp式を直接入力できない場合に採用する固定値テーブル（各BPでのpx実値）を明記し、目視確認による微調整を不要にする。
- **[g4|選択肢未確定]** §4.0 clamp入力tip
  - 問題: <code>4vw+8px</code> 形式が UI で直接打てない場合は、PC基準幅 <code>1280px</code> での計算値…を固定上限として置き
  - 修正: Studioの実UIで打てるかを確定し、本書として「Customに式を直書きする」または「固定上限+Mobile条件で縮小する」のどちらか一方を正とする。
- **[g4|任意・省略可]** §4.3 影 手順2
  - 問題: 視認性向上で任意付与する場合のみ Shadow Outset X:0 / Y:8 / Blur:20 …
  - 修正: 「カードに影は付けない（Shadowなし）」で確定する。任意付与の選択肢を削除。
- **[g4|選択肢未確定]** §4.3 影 チェックリスト
  - 問題: 基本どおり影なしにした（or ソフト影 .05 を採用したか明記）
  - 修正: 「影なしにした」のみのチェック項目にする。
- **[g4|推奨止まり]** §4.3 prob番号の代替callout
  - 問題: 番号Textを <code>blue</code> 単色にする…または 数字部分をImage化して貼る。…<strong>blue単色を推奨</strong>。
  - 修正: 「prob番号はblue単色にする（Image化はしない）」と確定する。
- **[g4|任意・省略可]** §4.3 svc点線コネクタの代替callout
  - 問題: 装飾のため<strong>省略も可</strong>。<strong>省略 or Box代替を推奨</strong>（Custom Code は使わない）。
  - 修正: 「svc点線コネクタは省略する」または「Boxで作る」のどちらか一方に確定する。
- **[g4|選択肢未確定]** §4.4 pill/label（ツリーおよび手順5）
  - 問題: pill/label (Text・English Label or 日本語13px/800)
  - 修正: pillラベルは日本語13px/800（ink）で組むと確定する（英字使用箇所のみ別途English Labelと範囲を明記）。
- **[g4|推奨止まり]** §4.4 em.y黄マーカー Free代替
  - 問題: <strong>推奨＝この背面黄色Box方式</strong>…完全一致が要る場合は Mini の Custom Code で <code>em.y</code> グラデ下線を移植。
  - 修正: 「em.yは背面黄色Box方式で作る」と確定し、Custom Code分岐を削除（必要なら背面Boxの高さ%・位置を実値で固定）。
- **[g4|曖昧値]** §4.5 Tablet Comp/Card
  - 問題: 親グリッド gap を <strong>22→16〜14px</strong> に。
  - 修正: Tablet gapを単一値に確定する（例: 16px）。
- **[s1|検証/実機裁量]** 5-1-8 写像注（推奨手順）
  - 問題: 540–381px帯で余白が詰まりすぎる崩れが出たら <strong>Mini(320)BP を追加</strong>して Mobile=767px相当値・Mini=380px相当値の2段に分ける。
  - 修正: 崩れ判定を持ち込まず、上の確定（Mobileに380値統合・BP追加なし）に従う旨だけ残し、条件分岐を削除する。
- **[s1|推奨止まり]** 5-1-5 推奨（Free代替 vs Mini）
  - 問題: <strong>推奨：【Free代替】</strong>（制作中は Free・Custom Code不可のため。 付録A）。
  - 修正: 「Free・制作中のためFree代替で実装する（Mini案は採用しない）」と1案に確定する。
- **[s1|推奨止まり]** 5-1-6 推奨（Free代替 vs Mini）
  - 問題: <strong>推奨：【Free代替】</strong>（Free でCustom Code不可のため）。
  - 修正: 「Free代替で実装する（Mini案は採用しない）」と1案に確定する。
- **[s1|任意・省略可]** 5-1-0（スクロール下境界線の扱い）
  - 問題: 扱いは <a href="#s1-7">5-1-7</a> でデザイン意図を確認する。確認が取れるまでは初期描画どおり<strong>透明1px</strong>で実装する。
  - 修正: 「scrolled状態は実装しない。下境界線は常時透明1px固定」と本書で確定し、確認待ち記述を削除する。
- **[s1|検証/実機裁量]** 5-1-7 step1（不要回答時）
  - 問題: 回答「境界線は不要（出さないのが意図）」→ 5-1-1 step5 のまま<strong>透明1px固定</strong>で確定。
  - 修正: 確認に依存せず「透明1px固定」を初期確定値とし、変更は別途指示があった場合のみとする。
- **[s2|選択肢未確定]** 5-2-7 D（VP Headline em 部分上書き, line 460）
  - 問題: サイズ <code>1.12em</code> 相当＝<code>24.6px</code>（または <code>25px</code>）に部分上書き
  - 修正: 「25px に部分上書き」と単一値に確定する（1.12em/24.6px は参考表記として括弧内に残す）。
- **[s2|選択肢未確定]** 5-2-8 C LW Marquee 両端フェード（line 624）
  - 問題: 各幅 <code>4%</code>（または約60px）、高さ100%
  - 修正: 「各幅 4%（高さ100%）」に固定し、px併記を削除する（または 60px に統一）。
- **[s2|選択肢未確定]** 5-2-9 案2（line 727）
  - 問題: または Mini で Custom Code の <code>order</code> 指定で対応
  - 修正: 案2採用時の実装手段を1つに固定する（例「Mini で Custom Code の order を使う」）。
- **[s2|検証/実機裁量]** 5-2-2 A overflow 注記（line 169）
  - 問題: 切れて良い／見せたいの判断は実機（5-2-10）で確定する
  - 修正: 本書は Hero=Hidden を既定とすると断定済みなので、「装飾円の見切れは許容する（Visible化しない）」と結果を確定する。
- **[s2|検証/実機裁量]** 5-2-8 D LW Card 画像 object-fit（line 631）
  - 問題: object-fit <strong>contain</strong>（注: フィット指定UIは要実機確認）
  - 修正: 「Image のフィットを contain に設定（UI名: ◯◯）」と具体操作まで確定し、要実機確認の括弧を削除。
- **[s2|任意・省略可]** 5-2-9 Tablet（line 712）
  - 問題: Tablet では高さ auto に戻し Section 高さは内容なりにしてもよい（実機確認）
  - 修正: 「Tablet は高さ auto に戻す」など一方に断定する。
- **[s2|検証/実機裁量]** 5-2-10 動作確認（line 750-751）
  - 問題: 収まらなければ Mini で Custom Code clamp を検討／見せたい円が切れる場合は Hero の overflow 方針を再検討
  - 修正: 判定基準と分岐後の確定対応を明記（例「見切れ1px以上なら overflow は Hidden 維持で円側を縮める」等、結果を一意化）。
- **[s3|曖昧値]** 5-3-2 / H2 Section テキストスタイル
  - 問題: H2 Section: Noto Sans JP / サイズは可変（Studioに 4vw+8px 形式の直接入力は不可 → 後述の写像値で固定px化。PC実効は52px）
  - 修正: Tablet=42px、Mobile=30px のように単一の確定pxを明記する
- **[s3|曖昧値]** 5-3-7 Tablet 手順4
  - 問題: H2 Section は中域サイズ（おおむね40-44px を固定値で）、Lead は 14-15px / lh1.9
  - 修正: H2 Section=42px、Lead=14.5px と単一値に確定する
- **[s3|曖昧値]** 5-3-7 Mobile 手順8
  - 問題: Approach Title（H2）=Mobile実効おおむね28-32px を固定値で、Approach Lead=14px / lh1.9
  - 修正: H2 Section（Mobile）=30px と単一値に確定する
- **[s3|推奨止まり]** 5-3-3 末尾 Free代替/Mini（グラデ透明の実装）
  - 問題: 厳密に transparent 起点にしたい場合は Mini以上で Custom Code … を入れる手もありますが、ネイティブのグラデで視覚差は出ないため上のネイティブが推奨です。
  - 修正: 「ネイティブ実装（blue-soft不透明度0%×2点）で確定。Custom Codeは使用しない」と明記する
- **[s3|検証/実機裁量]** 5-3-6 B-1 手順4 / バッジBorder太さ
  - 問題: まず 2.5px を入力して通るか実機確認 し、入力が通らない／小数が丸められる場合のみ 3px にフォールバック
  - 修正: 事前検証で値を1つに確定（例: 2.5pxで通ることを確認済として2.5pxに固定、もしくは全員3pxに統一）
- **[s3|検証/実機裁量]** 5-3-8 / 未確定・要実機確認
  - 問題: これらは断定せず実機/pricing比較表で確定すること。
  - 修正: 本書側で各項目を単一値・単一手順に事前確定し、実機確認を成果の分岐点にしない
- **[s4|選択肢未確定]** 5-4-2 checklist
  - 問題: セクション背景に4ストップグラデ（…）を、ネイティブ可否を実機確認のうえ適用 or Custom Code/暫定単色で実装し、採用方法を指示書に明記した
  - 修正: 確定した単一実装に対応するチェック文へ書き換える（例:「背景にCustom Codeブロックを貼付し#market背景を適用した」）。
- **[s4|推奨止まり]** 5-4-5 カードヘッダ plan mini（ホバー） / plan free
  - 問題: （推奨）… Hover を追加 → Move Y = -4px、… Duration = 0.2s。Hover の Free 可否は未検証 … Free段階ではホバー無しで作り、Mini切替後に Move Y -4px / 0.2s を付与する。
  - 修正: プラン固定前提で「ホバーを付与する／付与しない」を1つに断定（例:「本実装ではHover Move Y -4px / 0.2sを必ず付与する」）。
- **[s4|検証/実機裁量]** 5-4-5 callout note（バー単色代替）
  - 問題: 発注時に「バーは単色か2色グラデか」を未確定事項として申し送り。
  - 修正: 申し送りではなく本書で塗り方法を確定し、申し送り文を削除。
- **[s4|判断語]** 5-4-5 callout tip（2Box重ね）
  - 問題: よりグラデの見た目に寄せたい場合は … 推奨は実機確認→可ならネイティブ、不可なら単色
  - 修正: 2Box重ね案は採用しないと明記、または採用条件を客観基準で確定。
- **[s4|選択肢未確定]** 5-4-6 棒グラフ差分 up-bar-low（列1塗り）
  - 問題: 塗りは左カードの now バーと同値（#CDD5E5→#AEB9D0）→【ネイティブ可なら】線形180deg … /【未確定時】単色 bar-gray-2。
  - 修正: 「up-bar-lowは単色bar-gray-2で実装」と確定。
- **[s4|選択肢未確定]** 5-4-6 callout note（high バー代替）
  - 問題: 単色 yellow（#FFD166）または yellow-dk（#E8B84A）の塗りで代替（黄系の強調色は保持）。発注時に申し送り。
  - 修正: 代替色をどちらか一方に固定（例:「代替時は#FFD166を使用」）。
- **[s4|検証/実機裁量]** 5-4-5 カードヘッダ plan free（ホバーFree可否）
  - 問題: Hover の Free 可否は未検証（公式ヘルプに明示ゲートなし）。「可否未確定機能はMini切替後有効化前提で設計」が本プロジェクト方針。
  - 修正: プラン固定に従いホバー実装の有無を断定（例:「Mini前提でHoverを必ず実装」）。
- **[s4|任意・省略可]** 5-4-2 plan mini（背景Custom Codeの挿入位置）
  - 問題: ページ設定 → Custom Code → </head> 直前ブロックに次を1ブロックで貼る（30,000文字/ブロック上限内）
  - 修正: 背景実装を本手順に一本化すれば従属が消える（上位findingで解消）。
- **[s5|選択肢未確定]** 5-5-2 最大幅と中央寄せ（A方式／B方式）
  - 問題: Studioでは「最大幅820px＋中央寄せ」を次のいずれかで再現する。…（推奨・A方式）…（B方式）…
  - 修正: 「A方式（親 sec-service の水平中央寄せ）で行う。B方式は使わない」と1方式に確定する。
- **[s5|検証/実機裁量]** 5-5-2 svc-title リッチテキスト編集 tip
  - 問題: テキストは1つのText要素にまとめ…インライン編集で崩れる場合は【Free代替】として3パーツに分割します。
  - 修正: 「まず1Text要素で実装し、崩れの有無に関わらず分割しない（崩れたら本書5-5-2末尾の手動改行手順で対処）」と確定する。
- **[s5|検証/実機裁量]** 5-5-2 改行制御 Free代替（推奨）
  - 問題: PCで意図しない位置で折れる場合は、語の区切りに半角スペースを入れず、改行したい箇所のみ手動改行で制御する。
  - 修正: 改行を入れる確定位置（「融資」の直後で1回のみ手動改行）を明示し、それ以外の改行は禁止と書き切る。
- **[s5|任意・省略可]** 5-5-4 svc-icon-1 サイズ
  - 問題: 幅 48px、高さ 48px（固定。横並びで縮まないよう、必要なら Tablet/Mobileでも48px維持を明示）。
  - 修正: 「Tablet/Mobileでも幅48px・高さ48pxを明示指定する（縮小不可）」と必須化する。
- **[s5|検証/実機裁量]** 5-5-4 svc-icon-1 Inter未対応時
  - 問題: 未対応のままなら数字を Image/Icon 化します。
  - 修正: 「Interを§フォント章の手順で必ず追加してから適用する（未対応の場合の代替は行わない）」と確定し、Image/Icon分岐を削除する。
- **[s5|任意・省略可]** 5-5-0 ツリー svc-connector
  - 問題: svc-connector [Image] 十字点線コネクタ（PCのみ・装飾／省略可）
  - 修正: 「コネクタは5-5-6の推奨（Box2枚で十字）方式で必ず実装する」と一本化し、省略可表記を削除する。
- **[s5|検証/実機裁量]** 5-5-6 横線Box svc-conn-h 幅
  - 問題: 高さ 0 / 幅 32px（左右カードのすき間を渡せる長さ。すき間が広ければ伸ばす）。
  - 修正: 「幅=32px固定（gap20px前提）。伸縮させない」と確定する。
- **[s5|任意・省略可]** 5-5-6 末尾
  - 問題: 完全再現が必須でなければ簡略省略でも可。
  - 修正: 推奨方式に一本化したうえで「省略しない」と明記し、この一文を削除する。
- **[s5|検証/実機裁量]** 5-5-7 Mobile カードpadding warn
  - 問題: Mobile上書きが1枚だけにしか効かない場合は4枚それぞれに同じ上書きを適用します。
  - 修正: 「Mobileではカード4枚それぞれに padding 26/22 を個別上書きする（必ず4枚）」と無条件手順にする。
- **[s5|曖昧値]** 5-5-2 svc-sec-head Gap 補足/下マージン値の不整合
  - 問題: 下マージン24pxで吸収… / （5-5-7）svc-sec-head 下マージン=36px（PCの44pxから36pxへ）
  - 修正: 5-5-2に「svc-sec-head 下マージン（PC）=44px」を明記し、5-5-7の36pxと整合させる。
- **[s6|選択肢未確定]** 5-6-3 手順8 / リード値（5-6-3 冒頭）
  - 問題: サイズ式 clamp(28px,4vw+8px,52px)（PC実効52px。Studio がサイズ欄で clamp 直接入力不可なら PC=52px 固定、Mobile/Tablet 実効は 5-6-7 で確認）
  - 修正: 「Studio のサイズ欄に clamp 直接入力できる/できない」を本書で確定し、できない前提なら各BP固定値（PC=52px / Tablet=実効値 / Mobile=実効値）を全BP分明記して clamp式運用を廃する
- **[s6|選択肢未確定]** 5-6-7 写像方針（380px以下）
  - 問題: 380px 以下 → 3BP に 380px 段はないため Mobile に統合（数字 38px/ラベル 12.5px 採用）、または任意で Mini=320px を追加
  - 修正: 「Mini=320px は追加しない（Mobile の38px/12.5px で代替する）」または「必ず追加する」のいずれかに本書で一本化する
- **[s6|任意・省略可]** 5-6-7 Mini（任意）
  - 問題: 不要なら省略可（Mobile の 38px/12.5px で代替）。
  - 修正: Mini節を「実施しない」と確定（タイトルの『任意』表記も削除）し、当該チェックリスト項目を除去する
- **[s6|任意・省略可]** 5-6-7 Tablet 手順2
  - 問題: Mobile 節の Rec Grid 折返し・Rec Stat 幅50%・月桂樹/数字/ラベル縮小を Tablet へ複写（任意）。
  - 修正: 複写する/しないを確定（前項と整合）し『（任意）』を削除する
- **[s6|検証/実機裁量]** 5-6-8 仕上げチェック / 5-6-6 チェックリスト
  - 問題: 公開URLで月桂樹の左右クロップ位置・折返し・見出し折返しを実機確認：最終確認は公開URL）
  - 修正: 実機確認で許容する数値範囲と、ズレた場合の修正手順（具体的なposition/sizeの再設定値）を明記し、裁量での『微調整』を排除する
- **[s6|曖昧値]** 5-6-7 Record Title（Mobile表）
  - 問題: サイズ式 clamp(28px,4vw+8px,52px) 維持（固定値運用なら約 30px／下限28px以上）
  - 修正: 固定値運用時のMobile見出しサイズを単一値（例: 30px）に確定し『約』を削除する。または clamp式に一本化して固定値運用の選択自体を排除する
- **[s7|曖昧値]** 5-7-3 B 手順2
  - 問題: 右余白 ≒ 45px（180px×0.25）。
  - 修正: 「右余白（padding-right）＝ 45px（固定値で入力）」と「≒」を外して確定する。
- **[s7|曖昧値]** 5-7-3 B Free代替 (a)
  - 問題: vc-big を Image にして配置（高さ約180px相当）。
  - 修正: 「Image の高さ＝180px（固定）」と確定値で指定する。
- **[s7|判断語]** 5-7-4 B 手順3（ロゴ画像サイズ Free代替）
  - 問題: 縦が62%を超える縦長ロゴは Mini の Custom Code か個別に高さ上限を調整。
  - 修正: 「全ロゴとも vc-logo は 幅% 78・高さ auto・object-fit:contain で固定し、個別調整は行わない（縦長ロゴも contain に委ねる）」と一律手順に確定する。
- **[s7|曖昧値]** 5-7-6 Mobile（540px）/ vc-card 幅
  - 問題: 幅を %約47〜48 または実機幅から算出したpxに上書き。高さは幅×0.4 で再指定。
  - 修正: 「Mobileは vc-card 幅＝% 47（固定）、高さは幅×0.4 を px換算した固定値（例: 実機540幅・左右padding40で内幅460→(460−10)/2≒225px、高さ90px）を入力」と単一値に確定する。
- **[s7|検証/実機裁量]** 5-7-6 Mobile / vc-big
  - 問題: Mobileで画面幅に対し大きすぎる場合のみ、画像なら幅%で縮小、Mini…で font-size を下げる（視認性に応じて Live Preview 確認で調整）。
  - 修正: 「Mobileでは vc-big を 幅% 一定値（例: 60%）または font-size を固定値（例: 110px）に上書きする」と縮小値を確定する。
- **[s7|検証/実機裁量]** 5-7-2 C / 5-7-3 B / 5-7-4 A-2
  - 問題: Studioで clamp 不可なら PC実値 52px を入力し… ／ Custom Code（Head）で .vc-card{aspect-ratio:5/2…}（Mini以上・Free不可）
  - 修正: Studioの対応可否を本書側で事前確定し「clampは入力しPC実値52pxは使わない／aspect-ratioは使わず高さ94px固定」のように分岐を消して1手順にする。
- **[s7|推奨止まり]** 5-7-7 vc-big の作り方再掲
  - 問題: 【Free代替・推奨】グラデ込みで画像化（装飾円も1枚に描き込み）。【Mini】Custom Code head に .vc-big CSS を注入。
  - 修正: 5-7-3 の確定（画像化で作る）と整合させ、Mini分岐の併記をやめ画像化に一本化する。
- **[s8|選択肢未確定]** 5-8-1 手順1
  - 問題: 左ナビ→追加→セクション → 空の Section をドラッグして配置、または 左ナビ→追加→基本→Box を Base 直下に配置。
  - 修正: 「空の Section をドラッグして配置する」のみに確定（Box 案を削除）。
- **[s8|選択肢未確定]** 5-8-3 手順4 サブ項目
  - 問題: Studioに Grid 専用UIがある場合はそれを用い列数3を指定。無ければ横並び＋折返しで子3つを flex 等分にする。
  - 修正: 本書の正とする方式を1つに確定（例:「横並び＋折返し＋子 flex 等分で組む」）し、Grid UI 案は補足扱いにしない。
- **[s8|選択肢未確定]** 5-8-4 カバー Free代替
  - 問題: 写真を事前に 16:10 にトリミングして用意し…トリミングが難しい場合は §0.4「アスペクト比を固定する」のスペーサー画像法で 16:10 の器を作り
  - 修正: 本書で採る方法を1つに固定（例:「1200×750px に書き出した画像を幅100%・高さautoで入れる」）し、代替法は使わないと明記する。
- **[s8|検証/実機裁量]** 5-8-4 カバー 手順6 サブ項目
  - 問題: フィット/位置UIの有無は §8.2 の通り未検証。無い場合は画像を上寄り（35%）でトリミング書き出ししてアップロード。
  - 修正: UI有無を事前確定し、どちらの手順を採るか1つに固定する（または『必ずトリミング書き出しで対応』に統一）。
- **[s8|検証/実機裁量]** 5-8-4 リザルト 手順4 サブ項目
  - 問題: 垂直「ベースライン揃え」UIが無ければ垂直=下揃えで近似。
  - 修正: 本書で『垂直=下揃え（bottom）にする』と一律に確定し、UI有無による分岐をなくす。
- **[s9|任意・省略可]** 5-9-5 / プラン分岐（Free代替 callout, item-1 Toggle 状態差直後）
  - 問題: Studio Toggle で「影＋枠色の状態差＋矢印回転」まで条件スタイルで作れない場合は、影／枠の状態差は妥協し（一律 default 値）、矢印回転のみ Closed 条件スタイルで担保する運用でも可
  - 修正: 本書では『default（open）で影=0 16px 36px rgba(15,26,51,.08)・border=blue/lt、Closed で影=0 8px 24px rgba(15,26,51,.04)・border=line の状態差を必ず設定する』と確定し、妥協運用は記載しない（できない場合は Mini の details 実装へ進む、と単一分岐にする）。
- **[s9|推奨止まり]** 5-9-5 / プラン分岐（同上 callout 末尾）
  - 問題: 本章は基本 Studio ネイティブ Toggle を推奨
  - 修正: 「本章は Studio ネイティブ Toggle で実装する。Custom Code は使わない（Mini 完全再現が要求された場合のみ §10 の details 実装に切替える）」と断定する。
- **[s9|推奨止まり]** 5-9-5b / faq-q-badge-1 のプラン分岐（Free代替 callout）
  - 問題: Studio は擬似要素 ::before 非対応のため、実 Box ＋ Text で作ります（推奨）。
  - 修正: 「Q バッジは実 Box＋Text で作る（Custom Code は使わない）」と言い切る。
- **[s9|選択肢未確定]** 5-9-5b 手順17 / faq-q-arrow-1 右寄せ手法
  - 問題: 間の faq-q-text の幅を flex にして矢印を右端へ押し出す（または親を「両端揃え（Space Between）」
  - 修正: 「faq-q-text の幅を flex（伸長）に設定して矢印を右端へ押し出す」一方に確定し、（または…）を削除する。
- **[s9|選択肢未確定]** 末尾 注記（confidence）callout
  - 問題: Free で開閉が有効化できない場合は、Mini 切替後に有効化する前提で設計するか、暫定で Custom Code（Mini）による <details> 実装に切替えます。
  - 修正: 「Free で開閉不可の場合は Mini 切替後に Studio Toggle を有効化する（Custom Code は使わない）」と単一方針に確定する。
- **[s10|推奨止まり]** §5-10-2 見出し（5-10-2 セクション枠）／plan free recommended
  - 問題: （推奨）追加 Box で近似する。
  - 修正: 「Free 段階では必ず Free 代替（追加 Box ＋ Filter Blur）で 2 Box を作成する」と言い切る（省略・Mini を選ばせない）
- **[s10|任意・省略可]** §5-10-2 タイトル「背景グロー（装飾・任意）」／callout tip
  - 問題: 再現が手間ならこの 2 Box は省略可（淡い装飾で CV 影響なし）。
  - 修正: 「省略可」を削除し「2 Box を必ず作成する」または「作成しない」のいずれか一方に固定する
- **[s10|選択肢未確定]** §5-10-4 B／改行方針 (a)/(b)
  - 問題: よって以下いずれかを選ぶ
  - 修正: 本文どおり「(a) 改行なしで確定」と一文で言い切り、(b) は実施しない旨を明記する（チェックリストの『どちらを採るか確定済み』も削除）
- **[s10|選択肢未確定]** §5-10-6 A 手順1／callout tip
  - 問題: Form1/2/3 はフィールド構成・レイアウトの差のみ。どれを土台にしても可。
  - 修正: 「Form1 を土台にする」と単一を指定し、その上で 3 フィールドに整える手順に統一する
- **[s10|任意・省略可]** §5-10-6 A フォーム背景グロー／plan free recommended
  - 問題: Form の overflow:Hidden で角を丸く収める。手間なら省略可。
  - 修正: 「省略可」を削除し、Free 段階では必ず 2 Box を作成（または作成しない）に一本化する
- **[s10|選択肢未確定]** §5-10-6 F 手順7／円アイコン plan free recommended
  - 問題: ボタン内に小 Box（32×32px）＋テキスト ›、…。または Icon（chevron right）を円 Box 内に。
  - 修正: どちらか一方（例: テキスト › 入り 32×32px 小 Box）に固定し『または Icon…』を削除する
- **[s10|検証/実機裁量]** §5-10-7 Tablet／callout tip
  - 問題: Tablet 境界を 767px 近傍に調整するか、追加ブレークポイントを 768px に置きます（任意・要デザイン確認）。本手順は追加 BP なしとし、…
  - 修正: 「本手順は追加 BP なしで確定（768px 厳密再現は行わない）」と言い切り『任意・要デザイン確認』を別フェーズへ切り出す
- **[s10|判断語]** §5-10-7 Tablet チェックリスト
  - 問題: 768px 閾値の厳密再現が必要か（追加 BP 設計）を判断・記録済み
  - 修正: 「追加 BP は作成しない（768px 厳密再現は行わない）」と確定し判断項目を削除する
- **[s10|任意・省略可]** §5-10-8 検証チェック／最終項
  - 問題: 背景グロー（Section の青/黄・Form の黄/青）は Free 代替 Box で近似 or 省略。判断を記録
  - 修正: 全グローを「Free 代替 Box で必ず作成」または「作成しない」のいずれかに統一する
- **[s10|検証/実機裁量]** §5-10-9 未確定・注意／改行
  - 問題: 厳密再現が必要なら (b) 2Text 分割（§5-10-4 B）。要デザイン確認。
  - 修正: デザイン確認を本手順着手前に完了させ「(a) で確定」と本文に反映、未確定注記を除去する
- **[s11|曖昧値]** §2 (B)簡易再現の左カラム最大幅
  - 問題: よって最大幅は約 500px を目安にする。
  - 修正: 計算値496pxを単一確定値として「最大幅=496px」と明記（(B)を残す場合）。
- **[s11|曖昧値]** §9 ズレ帯対応 (a)フィデリティ優先
  - 問題: Tablet の基準値を 1024px 付近へ変更する、または Small(1140) など中間 BP を追加して切替点を 1024 / 767 に寄せる。
  - 修正: (a)採用時は「Tablet基準=1024px、追加BP Small=1140px」のように全数値を確定指定。
- **[s11|検証/実機裁量]** §8 右ブロック Powered by Studio・プラン依存注意
  - 問題: Mini 切替後にバナーを消す場合、このテキストを残すか消すかはクライアント判断で確定する。
  - 修正: 本書時点の制作方針として「ft-powered Textは常に配置する（Mini移行後の削除可否は別途指示があるまで残す）」と確定。
- **[s11|判断語]** §9 Tablet・(A)時の右セル空Box
  - 問題: (A) なら列指定を 1 列にし、右セルの空 Box は縦積み末尾に来る（必要なら非表示）。
  - 修正: 「Tabletでは右セル空Boxを display:none で非表示にする」と必須化（または「非表示にしない」と確定）。
- **[s12|任意・省略可]** §5-12-2 影 step（行173-175）
  - 問題: 同時2層が不可なら主層のみ＝下記1層目を採用し、残り1層は省略可。…外側影の多層可否は未検証 → 1層で妥協してよい
  - 修正: 「Studioで外側影2層が設定可能な場合は必ず2層（0/18/44 rgba(15,26,51,.22) ＋ 0/6/14 rgba(15,26,51,.1)）を入れる。2層がUI上不可能な場合のみ1層目を採用」と、判断条件を観測可能な事実（UIで2層追加できるか）で一意化する
- **[s12|検証/実機裁量]** §5-12-2 Free代替 注（行231）
  - 問題: On Appear の Free 可否は で未検証。発注前に pricing 機能比較表で確認。不可ならフェードなしの常時表示（fixed配置のみ）で代替。
  - 修正: 事前に可否を確定して本書に「Freeでは On Appear を使用する／使用しない」を断定で記載。代替時の挙動（常時表示か）も1つに固定する
- **[s12|検証/実機裁量]** §5-12-4 グラデ note（行351）
  - 問題: 再現困難なら Free代替 単色 blue-soft 背景＋下記グロー円Boxで色味を補う。
  - 修正: 「Studioのグラデは3ストップ・角度指定可能。必ず155deg 3ストップで設定する」と断定し、単色フォールバックは削除（または不可時の具体条件を明記）
- **[s12|選択肢未確定]** §5-12-7 取消線 Free代替（行432, 435-447）
  - 問題: 取消線を下記いずれかで再現… 推奨: 簡便性を取り RichText打消し線。傾き表現が要件なら追加Box線。
  - 修正: 「取消線は RichText打消し線で実装（色 strike-red・傾きは付けない）」のように1案に確定する。傾きが完成要件なら追加Box線で確定する
- **[s12|選択肢未確定]** §5-12-8b 「0」グラデ Free代替（行518）
  - 問題: グラデーション塗りが指定できればそれを使用…不可なら Image化…最低限の妥協は単色 perk-gold-2(#EE9F1E) または yellow-dk(#E8B84A)。
  - 修正: テキストグラデ塗りの可否を事前確定。妥協が必要な場合の単色も perk-gold-2 か yellow-dk のいずれか1つに固定する
- **[s12|判断語]** §5-12-11 768-840px Small BP（行640, 678）
  - 問題: 768-840px を厳密に一致させたい場合のみ、プロジェクト全体BPに Small（例:768）追加を検討。実害は小さいため通常は Tablet 一括全幅化で可。
  - 修正: 「Small BP は追加しない（Tablet一括全幅化とする）」と断定し、検討の余地を残さない
- **[t6|曖昧値]** 6.3.1 [A] 手順3
  - 問題: 速度 ＝ 60（px/s）を起点に、ロゴ列は品よく 40〜60px/s で調整（実機で見ながら微調整。公式デフォルト60px/s）。
  - 修正: 「速度＝60px/s を入力する」と単一値で言い切る（範囲・調整の余地を削除）。
- **[t6|検証/実機裁量]** 6.3.1 [A] 手順3
  - 問題: 実機で見ながら微調整
  - 修正: 「微調整しない。60px/s固定」と明記。
- **[t6|任意・省略可]** 6.3.1 [A] 手順6
  - 問題: 両端フェードマスク（左右グラデのぼかし）が必要なら、親 sec-trust 側に左右グラデの装飾 Box を absolute で重ねる
  - 修正: 付ける/付けないを断定（例「両端フェードは付けない」または「必ず付ける。幅48px・白→透明グラデ」）。
- **[t6|任意・省略可]** 6.3.1 [A] レスポンシブ
  - 問題: 速度は方向同様 ブレークポイント ごと変更可。
  - 修正: 「全BPで速度60px/s固定（BPごとに変えない）」と明記。
- **[t6|判断語]** 6.3.2 [F] 手順3
  - 問題: Close when clicking outside は FAQ では通常 OFF（複数同時開を許容）。
  - 修正: 「Close when clicking outside ＝ OFF にする」と言い切る（「通常」を削除）。
- **[t6|判断語]** 6.3.2 [F] 手順5
  - 問題: faq-icon の Closed 条件スタイル → 変形タブ → Rotate を既定アニメの回転量に合わせる（差分180度）。
  - 修正: Closed時Rotate＝45deg、Open時Rotate＝-135deg を具体値で指定。
- **[t6|選択肢未確定]** 6.3.3 [C] カードlift
  - 問題: Studioの Hover「Scale Up / Add Shadow」プリセットでも近似可だが、ここでは Y移動のため手動でMove Yを設定するのが忠実。
  - 修正: 「プリセットは使わず手動でMove Y＝-4px（vc-card -3px）を設定する」と一択に固定。
- **[t6|任意・省略可]** 6.3.4 [G] 見出し / 本文
  - 問題: 品質向上として各 section-title / カードに On Appear を付けてよい
  - 修正: On Appearを付けるか否かをプロジェクトとして確定（例「[G]は実装しない」または「全section-titleに必ず付与」）。
- **[t6|選択肢未確定]** 6.3.4 [G] 手順1
  - 問題: プリセット Fade In from Bottom（または Blur Fade In）。
  - 修正: 「Fade In from Bottom を選ぶ」と一択に固定。
- **[t6|曖昧値]** 6.3.4 [G] 手順2
  - 問題: 時間 0.4〜0.6s、カード群は Delay を段差（例 0 / 0.08 / 0.16s）で順次出現。
  - 修正: Duration＝0.5s、Delay段差＝0 / 0.08 / 0.16s と確定値で指定（「〜」「例」を削除）。
- **[t6|選択肢未確定]** 6.4.1 [H] Free代替
  - 問題: ① 点滅を諦め静的ドットにする（…）。② どうしても点滅させるなら Embed sandbox にドット要素＋<style>を内包。
  - 修正: 推奨どおり「Free段階＝静的ドットにする」と一択に固定（②は採らない）。
- **[t6|選択肢未確定]** 6.4.2 [I][J] Free代替
  - 問題: ① Embed sandbox に…内包するのが唯一忠実。② 妥協案: 見出しを Studio通常テキストにし…代替
  - 修正: 「①Embed sandboxで実装する（②妥協案は採らない）」と確定。
- **[t6|選択肢未確定]** 6.4.3 [K][L] Free代替
  - 問題: ① Embed sandbox にバナー全体（…）を内包。② JSトグルを使わず、Studioの On Appear…で近似
  - 修正: 「①Embed sandboxで実装する」と一択に固定。
- **[t6|曖昧値]** 6.3.2 [F] レスポンシブ
  - 問題: Mobile で質問文サイズ・padding を条件スタイルで縮小。
  - 修正: Mobile時の質問文サイズとpaddingを具体値で指定（例 font 15px / padding 14px 16px）。
- **[t6|選択肢未確定]** 6.3.3 [B] 見出し前文 / 重要callout
  - 問題: リンク遷移が必要なら a、押下フィードバックが必要なら button を選ぶ。
  - 修正: 本LPのCTAは a/button のどちらにするかを確定指定（例「全CTAは button タグにする」）。
- **[t7|検証/実機裁量]** §7.2.1
  - 問題: 中間帯専用の改行は再現対象から外し、「PC=1行 / Mobile=自然折返し」に丸める のが基本（中間帯の見栄えが問題なら Small を追加）
  - 修正: 「中間帯専用改行は一律再現しない（Small 追加もしない）」と言い切る
- **[t7|推奨止まり]** §7.2.2
  - 問題: 【推奨】該当語句を含む見出しを Default で nowrap／Mobile で normal に条件スタイル切替し…
  - 修正: 「nowrap/normal 切替＋max-width 確保の方式で実装する」と必須化し、代替手段を消す
- **[t7|曖昧値]** §7.2.2
  - 問題: テキストボックスの幅を語句が割れない幅に保つ（max-width をフレーズ単位で確保）
  - 修正: 対象フレーズごとの確定 max-width 値（px）を表で明示する
- **[t7|曖昧値]** §7.3.3 / §7.15.2 手順1
  - 問題: 下マージンは Safe Area 下端ぶん（環境により +約 34px）を上乗せ … 下マージンを実質 24px 程度に増やす
  - 修正: Free 時の下マージンを単一値（例: 下 36px）に確定し、Mini 時は env() 式を正本として固定
- **[t7|検証/実機裁量]** §7.3.3 末尾
  - 問題: Studio ネイティブには env(safe-area-inset-*) を直接入力する UI が確認できない（未検証）。
  - 修正: 検証を完了し「Free=下マージン◯px、Mini=Custom Code に下記 CSS」のどちらを採るか確定する
- **[t7|選択肢未確定]** §7.5.2 Free代替 / どうしてもネスト段落
  - 問題: どうしてもネストしたい場合は Mobile で各要素の表示順を成立させるレイヤー構成にあらかじめ寄せる
  - 修正: 「hero-inner 直下をフラットな縦並び子で構成する」一択に固定し、ネスト案を削除する
- **[t7|選択肢未確定]** §7.10.1 手順2
  - 問題: 列数を 3 列に（gap 12px）。Grid 列指定の UI で 3 等分（または各カードを %/flex で 3 列配分。
  - 修正: どちらの方法で 3 列化するかを 1 つに指定する（例:「Grid 列数 3 を使う」）
- **[t7|選択肢未確定]** §7.10.1 手順3
  - 問題: アスペクト比を固定する のスペーサー画像法 or Embed+CSS で維持
  - 修正: vc-card の比率維持手段を 1 つに確定する
- **[t7|曖昧値]** §7.11.2 / §7.5.2 等 共通見出し
  - 問題: 共通見出し Mobile 値（section-title clamp24-32 / lead 14 / sec-head mb36）
  - 修正: Mobile で section-title に入れる単一 px（例: 28px）を確定列挙する
- **[t7|検証/実機裁量]** §7.13.3 / §7.5.2 末尾 / §7.9.2 手順6
  - 問題: h2 が 28px で溢れる／フォーム余白が窮屈な場合のみ Mini(320) を追加 … 3 段なら 38px で許容
  - 修正: Mini(320) を追加するか否か、追加時の値を一律確定する（実機任意でなく固定）
- **[t7|曖昧値]** §7.15.2 手順1
  - 問題: 下マージンを実質 24px 程度に増やす（Free）か env(safe-area-inset-bottom) を注入（Mini）
  - 修正: Free 時の下マージンを単一 px に確定する
- **[t7|検証/実機裁量]** §7.4.2 手順7 / §7.5.2 手順5 等
  - 問題: ボタン高さが実効 44px に満たない場合は上下 padding を増やして 44px 高を確保
  - 修正: 44px 高を満たす確定 padding 値（上下◯px）を各ボタンに明記する
- **[t7|検証/実機裁量]** 末尾 note
  - 問題: 未検証 項目（…Free でのフォーム/Toggle 可否・Safe Area の env() 入力 UI）は実機/公式比較表での確認を要する。
  - 修正: これらを事前に検証して可否と採用手段を確定し、本書に結果を反映する
- **[t8|選択肢未確定]** §8 冒頭callout ①画像の圧縮
  - 問題: 形式＝WebP（または MozJPEG）／品質 80／Resize で幅を px 指定
  - 修正: 「形式＝WebP（.webp）で固定。品質80」と1形式に確定し、各ファイルの出力拡張子を明記する
- **[t8|曖昧値]** §8 冒頭callout ①目安
  - 問題: 目安: VC ロゴは長辺 600px 程度（特に United 1815px 等は要縮小）、事例カバーは幅 800px・品質 80
  - 修正: 各ファイルの目標長辺を1値で表に確定（例: United→長辺600px、GarnetCapital→長辺600px…）。「程度/目安」を削る
- **[t8|曖昧値]** §8.0.1 ロゴPNG行 対処
  - 問題: 透過維持のため PNG のまま寸法をダウンスケール（高さ 120px 程度を上限目安）
  - 修正: 「高さ240px（表示60pxの2倍）に固定して書き出す」等、各ロゴの出力高さを単一値で指定する
- **[t8|推奨止まり]** §8.0.1 高解像VCロゴ行 対処
  - 問題: contain 表示なので破綻はしないが Web 容量のためダウンスケール推奨
  - 修正: 「United/GarnetCapital/WaypointVenturePartners は長辺600pxに縮小して上げる（必須）」と言い切る
- **[t8|判断語]** §8.3 Studio手順（マーキー）手順3
  - 問題: 速度（既定 60px/s。完成イメージの体感に合わせて調整）
  - 修正: 「速度＝60px/s で固定（調整しない）」と確定する
- **[t8|任意・省略可]** §8.3 Studio手順（マーキー）手順3
  - 問題: Pause on hover を任意で ON。… 両端フェードが必要なら親 Box にグラデーションマスク Box を重ねる
  - 修正: 「Pause on hover＝ON にする」「両端フェードは行わない（マスク不要）」のように各々を1つに確定
- **[t8|任意・省略可]** §8.3 Studio手順（静的グリッド）手順3
  - 問題: Filter（§3.7・Saturation）で軽く適用（任意。なくても可）
  - 修正: 「saturate 0.95 を必ず適用」または「適用しない」のどちらかに固定する
- **[t8|判断語]** §8.5 Studio手順 手順2
  - 問題: Shadow（§3.6・Outset）で近似（X/Y/Blur/Color を調整。色は ink 系の半透明）
  - 修正: X/Y/Blur/Spread/Color(HEX+不透明度%) を各1値で明記（例: X0 Y4 Blur12 #0F1A33 25%）
- **[t8|推奨止まり]** §8.6.2 Mini代替
  - 問題: Free 代替（クリーン下線 or 事前焼き込み画像）を推奨。デザインリードに「ラフ縁は省略可」を確認
  - 修正: 「本書はクリーンな黄色マーカー下線（#FFD166・角丸わずか）で実装する」と1方式に言い切る
- **[t8|選択肢未確定]** §8.6.2 Free代替
  - 問題: クリーンな黄色マーカー下線（追加 Box…）で代替。または ラフ加工済みのストロークを透過 PNG/SVG として書き出し…Image 配置
  - 修正: 「クリーン下線 Box で実装する（焼き込み画像は使わない）」と一方に確定する
- **[t8|検証/実機裁量]** §8.9末 / §8.7冒頭 検証ソース
  - 問題: 未検証 表記の項目（画像フィット UI、lazy-loading 標準 UI、各機能の Free 可否）は実機エディタで確認のこと
  - 修正: 各未検証項目について本書のデフォルト採用案を1つずつ確定明記し、確認は事後記録のみとする
- **[t9|曖昧値]** §9.5 方式表（Free代替・本命）
  - 問題: 速度は <code>px/s</code>（既定60px/s）。<code>40〜60px/s</code> で品よく調整。
  - 修正: 表からは範囲を削除し「速度＝50px/s（固定）」に統一。手順§9.5-4の50px/sと一致させる。
- **[t9|判断語]** §9.5 方式表（Free代替・本命）
  - 問題: <code>40〜60px/s</code> で品よく調整
  - 修正: 「品よく調整」を削除し、確定値「50px/s」を入力する、と言い切る。
- **[t9|検証/実機裁量]** §9.2 callout tip 推奨
  - 問題: タイトハンドオフ時は ① クリーン下線へ簡略化も許容。design lead 判断事項として明記。
  - 修正: 「タイトハンドオフ時も②を実装する／簡略化しない」と固定するか、design lead の決定値を本書に転記して確定する。
- **[t9|検証/実機裁量]** §9.3 Free代替 妥協案
  - 問題: 出現は Studio の <strong>On Appear → Fade In from Bottom</strong>（…Free 可否は未検証＝Mini 切替後有効化前提）で代替
  - 修正: 「Free 期間は出現アニメなし（静的表示）、Mini 切替後に On Appear を付与」と動作を一意に固定する。
- **[t9|選択肢未確定]** §9.4 Free代替（推奨 vs 妥協案）
  - 問題: <strong>妥協案</strong>：「0」を<strong>静止の黄グラデ画像（Image）</strong>にし、光沢アニメは省略。
  - 修正: 「本書は Embed sandbox でカードごと移植を正とする。妥協案は採用しない」と確定する。
- **[t9|任意・省略可]** §9.1 機能・装飾表（5-5 サービス・十字点線）
  - 問題: 十字点線を1枚 SVG/PNG 化してグリッド中央に重ね Image 配置（<code>≤540px</code> 非表示）。装飾なので省略可。
  - 修正: 「§5-5-6 の Box 2枚方式で必ず実装する（省略しない）」と一意に指定する。
- **[t9|検証/実機裁量]** §9.1 手順下 checklist
  - 問題: 純装飾は省略可否を design lead に確認した（特に十字点線コネクタ・吹き出ししっぽ）
  - 修正: 本書で各純装飾の実装/省略を確定表として列挙し、確認待ち項目を残さない。
- **[t10|選択肢未確定]** 10.2 表示確認 Tablet
  - 問題: sec-header の Nav が表示維持 or 縮約（設計どおり）。横はみ出しなし
  - 修正: Tablet では Nav を「表示維持（6項目横並び）」と「縮約」のどちらにするか1つに確定し、その状態のみ合格と記す。
- **[t10|選択肢未確定]** 10.2 表示確認 Tablet
  - 問題: hero-inner の 2 カラム（左テキスト / 右 vp-card）が成立、または縦スタックへ移行が成立
  - 修正: Tablet の hero-inner は「2カラム維持」か「縦スタック」のどちらかに確定し、片方のみ合格条件とする。
- **[t10|選択肢未確定]** 10.2 表示確認 Tablet
  - 問題: svc-perk（Service Perk Banner）の左右 2 分割が維持 or 縦化が成立
  - 修正: Tablet の svc-perk は「左右2分割維持」と確定（または縦化と確定）し、合格状態を1つに限定する。
- **[t10|選択肢未確定]** 10.3 表示確認 Mobile
  - 問題: record-grid が 2×2 もしくは 1×4
  - 修正: Mobile の record-grid を「2×2」か「1×4」のどちらか一方に確定して記載する。
- **[t10|選択肢未確定]** 10.3 表示確認 Mobile
  - 問題: sec-footer が縦スタック・ft-nav が 2 カラム or 1 カラム（PC repeat(3,1fr) から上書き）
  - 修正: Mobile の ft-nav を「2カラム」か「1カラム」のどちらかに確定して合格条件を一意化する。
- **[t10|曖昧値]** 10.4 タッチ・操作性
  - 問題: 全リンク・ボタン（…）のタップ領域が概ね 44×44px 以上
  - 修正: 「タップ領域が 44×44px 以上（実測で下回るものは不合格）」と『概ね』を削り厳密な下限値にする。
- **[t10|選択肢未確定]** 10.4 タッチ・操作性
  - 問題: iOS Safari でアドレスバー伸縮時に Hero 高さが破綻しない（100vh ではなく dvh / auto 採用を確認）
  - 修正: Hero 高さは「dvh を採用」または「auto を採用」のどちらかに確定し、その設定値の一致を確認させる。
- **[t10|選択肢未確定]** 10.5 リンク・遷移
  - 問題: 外部遷移リンク（資料 DL・SNS 等）が新規タブ / 同一タブの設計どおり開く
  - 修正: 「資料DL・外部リンクは新規タブ（target=_blank）で開く」のように開き方を1つに確定して明記する。
- **[t10|任意・省略可]** 10.6 フォーム 送信先・通知
  - 問題: 必要なら Google Sheets 連携を有効化（リード一覧化・命名 [Project]_[Form]）
  - 修正: 本案件で Google Sheets 連携を「有効化する／しない」を確定し、命名規則の具体名（実プロジェクト名）まで指定する。
- **[t10|選択肢未確定]** 10.6 フォーム 送信後挙動
  - 問題: 回答者への自動返信メールは Studio ネイティブ非対応。スコープに含むなら Google Sheets ＋ Zapier ＋ Gmail を構築済み（含まないならクライアント合意済み）
  - 修正: 自動返信を「スコープ外（クライアント合意済み）」に確定するなど、本書時点でどちらかに決め切る。
- **[t10|検証/実機裁量]** 10.6 プラン分岐 callout
  - 問題: フォームのメール通知・Google Sheets 連携・reCAPTCHA（Apps 連携）が Free で使えるかは 要確認。
  - 修正: Free での各機能の可否を事前に検証して本書に確定記載（可/不可と切替後対応）し、作業者の確認に委ねない。
- **[t10|推奨止まり]** 10.6 プラン分岐 callout（Mini 推奨）
  - 問題: 推奨：通知 / Sheets / reCAPTCHA が制限される場合は、納品後 Mini 切替後に有効化する前提で進め
  - 修正: 「通知/Sheets/reCAPTCHA は Mini 切替後に有効化する（Free 段階では設置のみ）」と断定形にする。
- **[t10|曖昧値]** 10.7 メタ
  - 問題: サイト <title> 設定（例: …、全角 30 字目安・重要 KW＝補助金 / 申請支援を前方）
  - 修正: 確定タイトル文字列そのものを1つ明記（または「全角30字以内、上限厳守」とし採用文を固定）する。
- **[t10|曖昧値]** 10.7 メタ
  - 問題: meta description 設定（全角 70〜120 字・先頭に KW）
  - 修正: 採用する description 全文を本書に確定記載し、作業者は貼り付けるだけにする。
- **[t10|選択肢未確定]** 10.8 法務
  - 問題: 🟡 12 社 / 13 社目標の差分確定（legal §7.1）: 12 社で確定 or 1 社追加待ちをクライアントに確認
  - 修正: 掲載VC社数を「12社で確定」と決め切る（または13社確定）よう本書で一意化する。
- **[t10|曖昧値]** 10.8 法務
  - 問題: 🟡 着手金 0 円表記（…）への注釈（※）追加・統一注釈の配置（legal §6.1）
  - 修正: 注釈の確定文面と配置箇所（どの表記の直後に置くか）を1つに指定する。
- **[t10|検証/実機裁量]** 10.9 アクセシビリティ
  - 問題: カラーコントラストが WCAG AA を満たす（…薄背景上の小サイズ文字を重点確認）
  - 修正: 対象テキストと背景の具体ペア、合格比（通常4.5:1/大文字3:1）を列挙し、計測ツールと合格値で機械的判定にする。
- **[t10|判断語]** 10.10 パフォーマンス
  - 問題: Lighthouse Performance 80+ を目標に計測（モバイル基準）
  - 修正: 「Lighthouse Performance（モバイル）80以上を合格とし、未満なら画像/Embedを見直し再計測」と合否基準と対処を確定する。
- **[t10|判断語]** 10.10 パフォーマンス
  - 問題: 月間 Visitor 上限（Free / Mini＝2,000）に対し、広告流入見込みが超過しないか確認（超過見込みなら Personal 以上を検討）
  - 修正: 想定月間流入数の確定値と、それに対するプラン（例: 超過見込みのため Personal を選定）を本書で確定する。
- **[t11|曖昧値]** 11.1b 固定ヘッダー
  - 問題: z-index は 100 程度で妥当（値域 -9999〜9999）
  - 修正: 「ヘッダーの z-index = 100（固定値）」と単一値で言い切る
- **[t11|曖昧値]** 11.1a 対処（Studio）Free代替
  - 問題: 全画面オーバーレイにしたい場合はドロワー Box を fixed＋z-index 高に
  - 修正: ドロワーの z-index を具体値（例: 200）に確定し、序列「ドロワー200 > ヘッダー100 > float-perk 50」と数値で固定
- **[t11|曖昧値]** 11.12c 対処
  - 問題: フォーム最下部に float-perk バー高ぶんの下余白を Mobile で確保
  - 修正: Mobile のフォーム下余白を具体値（例: 72px）で固定指定する
- **[t11|選択肢未確定]** 11.2c フォームカード装飾
  - 問題: vp-badge はカード内 padding を 0 にした最上部の全幅 Box として作り
  - 修正: vp-badge のカード内 padding と badge 内 padding を一意に定義（例: カード内余白0・badge 内 padding 13px 14px）
- **[t11|検証/実機裁量]** 11.3b 点線円形バッジ
  - 問題: きれいに出ない場合はバッジを SVG 画像化（等倍縮小に強い）／dotted がにじむ場合は appr-step を SVG 画像に置換
  - 修正: 「appr-step は dotted 円 Box で作る」または「SVG 画像で作る」のどちらかに一本化して言い切る
- **[t11|任意・省略可]** 11.2e ロゴマーキー Free代替
  - 問題: 縁フェードマスク…両端にグラデ Box を重ねる／完全一致が難しければ省略可
  - 修正: 「縁フェードは両端グラデ Box で必ず実装する」または「縁フェードは実装しない」を本書として確定
- **[t11|判断語]** 11.7a 4列→3列→2列
  - 問題: 厳密に 480px 境界が必要なら Mini(320) を足して 2 段化
  - 修正: VC グリッドは「Tablet 3列・Mobile 2列、Mini は追加しない」等に確定
- **[t11|検証/実機裁量]** 11.8b カバー写真 Free代替
  - 問題: Studio のフィット指定 UI（object-fit 相当）の有無は 未検証／Studio のフォーカル指定が粗い場合は…事前書き出しして回避
  - 修正: 検証結果を本書で確定し「c-cover は 16:10・上寄り35%で事前書き出した画像を使う」等、単一手順にする
- **[t11|検証/実機裁量]** 11.8c チェックリスト
  - 問題: フォーカル設定可否を実機確認→不可なら画像を事前トリミング
  - 修正: 事前トリミング画像を正とする等、分岐させず単一手順に固定
- **[t11|検証/実機裁量]** 11.9a FAQ 640px
  - 問題: 541–640px の中間見えは許容し、実機で破綻が無いか確認
  - 修正: 541–640px帯の許容を確定事項とし、追加調整は行わない（または具体的合否基準を明記）
- **[t11|選択肢未確定]** 11.12a float-perk Free代替
  - 問題: Free 段階は「常時表示の静的バー」か「一旦オフ」のどちらかにし
  - 修正: 「Free 制作中は float-perk を非搭載（オフ）にする」等、片方に確定（チェックリストは『Free制作中は非搭載』とあり本文と齟齬）
- **[t11|選択肢未確定]** 11.13-7 横断チェック表
  - 問題: clamp は固定 px or BP 別へ展開
  - 修正: 「clamp は BP 別（PC/Tablet/Mobile）の固定 px 値へ展開する」と方式を一意に決める
- **[t11|検証/実機裁量]** 11.13-3 横断チェック表
  - 問題: BP 別に改行位置を手動調整／実機で 1 語 1 語確認
  - 修正: 各見出しの改行挿入位置（どの語の後で折るか）をBPごとに明示列挙する
- **[t11|検証/実機裁量]** 11.0 注意列
  - 問題: Mobile で 2 列指定し 480–540px の見え方を許容
  - 修正: VCの480px対応方針（Mini追加 or 許容）を本書で一意に確定し許容範囲を明記
- **[t11|推奨止まり]** 11.1a Free代替
  - 問題: 推奨。追加 → 基本タブ → インタラクション → Toggle を使い
  - 修正: 「ハンバーガーは Toggle で実装する」とプラン方針に沿って確定（Free確定ならMiniは不使用と明記）
- **[t11|推奨止まり]** 11.12a float-perk Mini代替
  - 問題: 推奨。カード全体を Custom Code/Embed（HTML+CSS+JS）で丸ごと注入が最優先・最確実
  - 修正: プラン方針に従い「float-perk は Mini の Custom Code で実装する」と確定し代替を補足扱いにする

### severity 低

- **[g0|曖昧値]** §0.3 本文・図 / 写像表 前文
  - 問題: 近い帯（767px・640px・480px・380px あたり）は同じ BP にまとめて寄せ／不揃いな切替幅（1280px・768px・767px・1024px・640px・480px・380px あたり）
  - 修正: 「あたり」を外し、各帯の写像先BP（Small=1024 / Tablet=840 / Mobile=540）に集約すると確定済みである旨だけ残す（個別px値は各§5章で確定、と参照を明記）。
- **[g0|選択肢未確定]** §0.4 早見表 アスペクト比固定 dd
  - 問題: 任意比率（16:10等）を厳密に → ①ダミー画像を Imgモードで…または Embedボックスに縦横比固定（aspect-ratio:16/10）を直接CSS記述
  - 修正: どちらか一方を正手順に固定する（例「比率固定はダミー透明画像方式で行う。Embed方式は使わない」）。
- **[g0|検証/実機裁量]** §0.4 早見表 可変フォント dd / 列比率 dd
  - 問題: テキストタブのサイズは 固定 px を入力（当面）／親＝横並び＋各子のサイズ幅＝flex に比率数値（例 1.1 / 1）で再現
  - 修正: 「フォントサイズは全BPで固定pxを入力し、可変(vw/clamp)は使わない」と当面表現を外して確定する。
- **[g0|検証/実機裁量]** §0.2 操作 step2 末尾 / §0.4 早見表 BP追加 dd
  - 問題: ※ 一部の旧UIでは「上部 Responsive メニュー → Customize Breakpoints → ＋」の場合あり
  - 修正: 標準経路（下部デバイス幅バー → Edit mode）を正とし、旧UIに当たった場合の対応は付録に分離して本文の判断負荷を下げる。設定後の到達状態（Small=1024 有効）をチェック条件として明記。
- **[g0|判断語]** §0.4 早見表 主軸方向に伸ばす dd
  - 問題: うまく伸びない時は親の整列＝Stretch ＋ 子サイズ＝auto でも可
  - 修正: 正手順を一つ（例「子の幅/高さ単位を flex=1 にする」）に固定し、代替策は『それで伸びない場合のみ Stretch+auto』と条件と順序を明確化する（『でも可』を削除）。
- **[g0|検証/実機裁量]** §0.4 実装後 step1・§0.3 写像表 380px行
  - 問題: 実機（スマホ360–414px / タブレット768–1024px / PC）でスクロール確認／360–380px 実機で破綻なきよう Mobile 値を保守的に
  - 修正: 確認は手順として残しつつ、Mobile の各値（左右余白16px・フォント等）は各§5章の確定値を変更しないと明記し、『保守的に』の裁量調整を禁止する。
- **[g1|選択肢未確定]** 1.1 step1（line80）
  - 問題: エディタ画面外の左右どちらかのグレー余白をクリックしてページ設定パネルを開く
  - 修正: 「画面外の左側グレー余白をクリック」と片側に固定するか、「左右どちらでも同一パネルが開く」と結果一致を明記する。
- **[g1|推奨止まり]** 1.1 step3 補足 callout（line84）
  - 問題: 推奨は全角30字目安・重要KW前方。上記は記号込みで超過するため、検索結果での見え方を優先するなら別案を法務/コピー確定後に検討する。本章では上記コピーを正として登録する。
  - 修正: 「本章では上記タイトル文字列をそのまま登録する。文字数・別案の検討は本章では行わない」と明確に断ち切る（実質確定済みなので除外可だが念のため低）。
- **[g1|検証/実機裁量]** 1.1 step5（line90）
  - 問題: lang 属性の出力経路は要実機確認（confidence：未検証）として扱う … 公開後に出力HTMLの <html lang> が ja になっているかを実機で確認する
  - 修正: サイト言語＝日本語に設定する手順を確定値として記載し、確認は公開章へ完全委譲（本章の完成物条件から外す）と明記する。
- **[g1|曖昧値]** 1.1 step6（line95）
  - 問題: 48×48px 以上（96/144 等）、SVG含む全形式可
  - 修正: 登録するfaviconサイズを単一値（例：512×512px PNG）に確定する。元画像受領仕様としても単一寸法を指定する。
- **[g1|選択肢未確定]** 1.3 step5 ③（line241）
  - 問題: Lato は使用箇所が無ければ … Delete（どこかで使用中なら [Replace and Delete Font] で Noto Sans JP へ置換）
  - 修正: 「新規プロジェクト初期状態では Lato は未使用。Font Settings › 三点 › Delete で削除する」と単一手順に確定する。
- **[g1|検証/実機裁量]** 1.3 step5 ③（line241）
  - 問題: Lato が最上部にある場合は ① のドラッグで Noto Sans JP を最上部へ
  - 修正: 「①で Noto Sans JP を必ず最上部にドラッグする（Lato の初期位置に関わらず）」と無条件手順にする。
- **[g1|判断語]** 1.2 写像表 380px行（line197）
  - 問題: Mobile（必要なら Mini 320 追加を検討）
  - 修正: 本章では Mini 320 を追加しないことを確定。追加が要る場合の条件と値は別章で確定する旨に統一する。
- **[g1|推奨止まり]** 1.1【Free代替】callout（line137）
  - 問題: 本章では標準UIでの設定を推奨（Free完結・MPA新基盤でも有効）
  - 修正: 「本章は標準UI（Page設定）で設定する。Custom Code方式は使わない」と言い切る。
- **[g1|推奨止まり]** 1.4【Free代替】callout（line487）
  - 問題: Studio の Fill グラデーション（Box外観）で再現可能なため、標準Boxのグラデ塗りを推奨（Embed不要）
  - 修正: 「背景グラデは標準Boxの Fill グラデ塗りで再現する（Embedは使わない）」と確定する。
- **[g1|任意・省略可]** 1.4 step1 サブ項目（line290）
  - 問題: §2 デザイントークンで bg を登録後に割当。本章では HEX 直接指定でも可
  - 修正: 本章での手順を1つに確定（例：本章では #FCFDFF を HEX 直接指定し、§2登録後に bg スタイルへ差し替える、と順序を固定）。
- **[g1|検証/実機裁量]** 1.4 step5/再掲 step7（line319, 483）
  - 問題: スクロール出現・ホバー等は Base 直下以下の各 Box（Float Perk 含む）に付ける
  - 修正: 本章成果物を「Base直下にfixed・初期hidden枠を置くまで」と明示済みのため確定的。表示制御の値は別章で確定と明記すれば可（実質確定、低）。
- **[g1|検証/実機裁量]** 1.1 step5 / 関連ファイル要確認（line90, 512）
  - 問題: Studioサイト言語＝日本語設定時に出力HTMLの <html lang> が ja になるか（… confidence：未検証 → 公開後に実機確認）
  - 修正: 本章ではサイト言語＝日本語に設定する操作のみを確定手順とし、出力検証と是正は公開章の確定チェック項目に移す。
- **[g2|曖昧値]** §2.1-A brand 系表 #3 / §2.5 角丸表 等の「箇所」表記ではなく §2.4 カード内 padding（PC）行
  - 問題: <code>32px</code>（prob/appr/mkt）/ 36px×32px（svc）
  - 修正: 本表に列挙していないカードの padding は各セクション章の指定に従う旨を明記し、本章だけでは確定しないことを断言（または全カード分を列挙）。
- **[g2|判断語]** §2.2 フォント追加手順 注（Font List ラベル）
  - 問題: ラベルを <code>和文</code>（Noto Sans JP）/ <code>英字</code>（Inter）に設定して整理
  - 修正: 「ラベルを必ず『和文』『英字』に設定する（任意ではない）」と明記、または見た目に無関係である旨を断り省略可と明示。
- **[g2|任意・省略可]** §2.1 tip『別経路（要素から登録）』
  - 問題: 先に要素を置いてから登録する場合はこちらでもよい（結果は同じスタイルとして全体共有される）。
  - 修正: 基本は§2.1の番号手順（スタイルパネル）を正とし、別経路は『同一結果になる代替』と位置づけを断言（完成物不変なので低）。
- **[g2|曖昧値]** §2.6 シャドウ台帳 表「shadow-cta」行
  - 問題: <code>0 10px 24px rgba(74,125,232,.32)</code>（8–12px レンジ・.28〜.32）
  - 修正: 括弧のレンジ注記を削除し『0 10px 24px rgba(74,125,232,.32) に統一』と断言（主値が確定しているので低）。
- **[g2|検証/実機裁量]** §2.0 前提『HEX 入力は省略形…正規化される』
  - 問題: HEX 入力は省略形（<code>#fff</code>）・全角も正規化される（2026 1-2 月アップデート）。
  - 修正: 現状で確定済み（『本台帳どおり 6 桁または 8 桁の半角 HEX で入力』）のため対応不要。残すなら正規化の説明を補足扱いと明示。
- **[g3|推奨止まり]** §3.2 操作手順 手順1
  - 問題: 高さ = auto: 親高さは px でなく auto 推奨。折返し差を吸収）
  - 修正: 「高さ = auto に必ず設定する（px は使用しない）」と言い切る
- **[g3|検証/実機裁量]** §3.3 操作手順（FloatingCTA-Perk）手順3
  - 問題: Studio では 変形（Transform）タブ → アニメーションで On Appear（スクロール出現）を使う想定。本章では存在と重ね順のみ確定し、出現アニメは Floating CTA 章で詳述
  - 修正: 本章ではアニメ未設定（重ね順・座標のみ）と明記し、設定は完全に別章へ限定する旨を断定形にする（現状でほぼ確定だが『想定』の語を削る）
- **[g3|選択肢未確定]** §3.3 Free代替（しっぽ）
  - 問題: 三角形 SVG（または PNG）を Image 化して各 bubble の左右辺に absolute 配置するのが確実
  - 修正: 「SVG で書き出して Image 化する」のように一方に固定する
- **[g3|選択肢未確定]** §3.4 Free代替（装飾円）
  - 問題: 追加 Box（円 = 角丸 50%、を absolute で背面（重ね順を親内で下げる/レイヤー順で背面）に配置
  - 修正: 背面化の方法を1つに固定（例:「重ね順を 0 にしコンテンツを 1 にする」）
- **[g3|任意・省略可]** §3.3 設計の要点 2項目目
  - 問題: Studio では「装飾 Box の重ね順 0」「コンテンツ Box の重ね順 1」で同じ前後関係を作る（負値は使わず、レイヤーパネルの並び = 装飾を上 = 背面で代替可）
  - 修正: 「装飾=重ね順0／コンテンツ=重ね順1 に必ず設定する」と単一方式に確定（代替可の記述を削除）
- **[g3|選択肢未確定]** §3.5 注意（callout warn）
  - 問題: 最終確認は Live Preview / 公開 URL（vw/vh/固定/sticky/backdrop-filter はエディタ内で再現されない）
  - 修正: 確認手段を1つに固定（例:「Live Preview で確認する」）
- **[g3|検証/実機裁量]** §3.5 操作手順 手順9 / §3.4 全体
  - 問題: 個別セクション内グリッドの 1 カラム化（…）は各セクション章で実施。本章では外殻と Header/cta-inner/sec-bridge の個別 padding のみ
  - 修正: 委譲先章の参照を明示し、本章で確定する範囲を箇条書きで限定（スコープ境界を明記済みなら問題なし）
- **[g3|判断語]** §3.3 Free代替（しっぽ）
  - 問題: 各 bubble ごとに、border で作る三角を近似する細い横向き三角形を作る
  - 修正: 近似ではなく確定値（colored 13px / 透明 7px / rotate b1:17° b2:-17° b3:-18° b4:18° / 色 --blue）をそのまま再現する手順に統一（既に値はあるため『近似』語を削除）
- **[g3|判断語]** §3.4 共通配置
  - 問題: 親側に position:relative 相当（Studio は親に絶対配置の子があると基準化される）
  - 修正: 「親 Box に絶対配置の子を置くことで基準化される。明示の relative 設定は不要」と挙動を断定形で確定する
- **[g3|判断語]** §3.3 callout warn（Firefox フォールバック）
  - 問題: フォールバックとして背景色のアルファを .92 のまま残すこと（ぼかしが効かなくても半透明白で可読）
  - 修正: 「背景色 rgba(252,253,255,.92) を Firefox 用にもそのまま使用する（変更しない）」と確定形にする
- **[g4|推奨止まり]** §4.1 矢印円の実装判断callout
  - 問題: 矢印円を疑似要素で再現したい場合は Custom Code（Mini以上）だが…<strong>Box方式を推奨</strong>。
  - 修正: 「矢印円は必ずBox方式で作る（Custom Codeは使わない）」と言い切る。
- **[g4|推奨止まり]** §4.4 点滅ドットcallout
  - 問題: 明滅不要なら無地のまま静的表示。<strong>推奨=点滅を省略</strong>（静的ドットで十分）。
  - 修正: 「ドットは静的表示（点滅は実装しない）」と言い切る。
- **[g4|選択肢未確定]** §4.4 手順1 Comp/Section-Head 幅
  - 問題: サイズ→幅: <strong><code>flex</code></strong>（or % 100%）
  - 修正: 幅は flex（または %100%）のどちらか一方に統一する。
- **[g4|曖昧値]** §4.4 em.y黄マーカー Free代替
  - 問題: 黄色Box（塗り <code>yellow</code>・高さ＝包みBoxの約40%・下寄せ…
  - 修正: 高さを単一の確定値にする（例: 行高に対し高さ40%＝具体pxまたは固定%で言い切る）。
- **[g4|判断語]** §4.5 Tablet Comp/Section-Head
  - 問題: <code>Comp/Section-Head</code>: 変更最小。padding 左右は 40px のまま可。
  - 修正: Tabletでは Section-Head は一切変更しない（PC値継承）と明記する。
- **[g4|検証/実機裁量]** §4.5 Mobile Comp/CTA-Primary
  - 問題: 矢印円 <code>cta-pri/arrow</code>: <strong>28px</strong>（32→28）、矢印アイコン font <strong>12px</strong>（13→12）、右位置 right14（参考）
  - 修正: 右位置を確定値で指定する（例: right 14px と言い切る）か、親padding右寄せで配置と明記して位置指定を不要にする。
- **[g4|検証/実機裁量]** §4.5最終 Live Preview callout
  - 問題: Mobile実機でCTAのタップ後表示と H2/Lead の流体サイズを確認する。
  - 修正: 実機で差異が出た場合の調整基準（許容範囲・上書き値）を事前に確定し、確認は合否判定のみに限定する。
- **[g4|判断語]** §4.0 リード本文 / clamp tip
  - 問題: PC基準幅 <code>1280px</code> での計算値（4vw=51.2px+8px=59.2px→上限52pxにクランプされ実質52px）を固定上限として置き
  - 修正: 固定上限を採る場合のTablet/Mobile各サイズの確定値を本書に明記し、中間幅の縮小手順を一意化する。
- **[s1|推奨止まり]** 5-1-8 Mobile step3（Toggleトリガー・Free可否）
  - 問題: Toggle は <strong>Click トリガー推奨</strong>（タッチ誤動作回避）、…Toggle の Free 可否は未検証のため、<strong>Mini切替後に有効化する前提</strong>で設計し
  - 修正: ハンバーガー不採用を確定すれば本項は不要。採用する場合は「Clickトリガーにせよ」と断定する。
- **[s1|検証/実機裁量]** 5-1-2 step9 / warn直前
  - 問題: <strong>最終確認は公開URL</strong>（sandbox type は <code>DOMContentLoaded</code> 不発火等の制約。
  - 修正: 確認手順は残しつつ「ずれた場合はheight=42・width未指定・fill #0F1A33を再確認する」等、是正を確定手順化する。
- **[s1|曖昧値]** 5-1-3 step6 / step7
  - 問題: 各項目のテキストとアンカー先（下表のコピー・href を厳守）
  - 修正: （修正不要・確定済み）
- **[s1|判断語]** 5-1-1 step3（幅auto選択）
  - 問題: より、fixed の幅は<strong>左右0で取るのが安全</strong>
  - 修正: （実装は確定済み。文言から「安全」を削り「左右0で取る」と断定すれば曖昧さ皆無）
- **[s1|選択肢未確定]** 5-1-1 step8
  - 問題: Base 直下の本文先頭要素（Hero）に上方向の余白を確保するか、Header の次に高さ78pxのスペーサー Box を入れる。本手順書では <strong>Hero セクション側の上パディングで吸収</strong>する方針
  - 修正: （確定済み・修正不要）
- **[s2|曖昧値]** 5-2-2 B 角丸近似（line 178）
  - 問題: 各コーナー個別の角丸（例 40%/50%/38%/48%）で近似 Box を2枚置く
  - 修正: 「例」を外し、ステップ4/4の確定値（Blob1=40/50/38/48、Blob2=50/40/55/45）のみを正とする旨を明記。
- **[s2|任意・省略可]** 5-2-4 Eyebrow 数字オフセット（line 295）
  - 問題: テキストタブの位置/オフセットで <code>-2px</code>（ネイティブで不可なら無視可、視覚影響小）
  - 修正: 「ネイティブで可能なら-2px、不可なら0で確定（無視）」と一方に断定する。
- **[s2|任意・省略可]** 5-2-7 D 必須印※ Free代替（line 486）
  - 問題: ベースライン <code>vertical-align:1px</code> 相当（ネイティブ不可なら無視可）
  - 修正: 「不可なら0で確定」と一方に固定する。
- **[s2|任意・省略可]** 5-2-8 D LW Card 画像（line 631）
  - 問題: 彩度 <code>saturate(.95)</code>は Filter（Saturation）で近似（省略可）
  - 修正: 「Saturation を 95% に設定する」または「彩度補正はかけない」のどちらかに確定する。
- **[s2|検証/実機裁量]** 5-2-8 C 速度（line 621）
  - 問題: <strong>速度 <code>58px/s</code></strong>（デフォルト60に近い）。微調整は実機プレビューで。
  - 修正: 「58px/s で固定し、微調整はしない」と断定する（調整を許すなら許容範囲±値を明記）。
- **[s2|判断語]** 5-2-9 Mobile（line 737）
  - 問題: <code>LW Label</code>: padding左右など極小調整。
  - 修正: Mobile の LW Label の padding 左右を具体px（例 左右 12px）で確定する。
- **[s2|任意・省略可]** 5-2-9 Mini(320)（line 741-743）
  - 問題: 必要なら Mini(320) を追加して H1/Sub/Headline をさらに縮小。不要なら省略可
  - 修正: 「Mini(320)は追加しない（Mobile値を継承）」と省略を既定にするか、追加時の具体値を明記する。
- **[s2|任意・省略可]** 5-2-9 写像（line 701, 706）
  - 問題: ~380px 極小（必要なら Mini(320) 追加）／必要なら追加BPで1024相当の前倒しも可
  - 修正: 本LPで使うBPは Default/Tablet/Mobile の3つに固定する旨を断定（追加BPは作らない）。
- **[s2|判断語]** 5-2-5 sweepGrad Free代替（line 343）
  - 問題: スイープ停止後の最終見え色は、各強調語ごとにおおむね: …blue / …blue / …navy
  - 修正: 「おおむね」を削除し、sweep=blue / em.b=blue / em.n=navy を確定色として断定する。
- **[s2|推奨止まり]** 5-2-5 fvUnderline 下線マーカー（line 348-349）
  - 問題: <span class="badge">Free代替</span>（推奨表示なし）／ <span class="badge">Mini recommended</span>
  - 修正: 「Free段階は必ず Free代替のグラデバーで実装し、Mini切替時に Embed へ差し替える」と手順として断定する。
- **[s2|曖昧値]** 5-2-9 Mobile VP Headline（line 736）
  - 問題: em部 <code>1.08em</code>（≒<code>19.4px</code>）
  - 修正: Mobile の em 実値を単一px（例 19px）で確定する。
- **[s3|曖昧値]** 5-3-6 D / hover アニメーション
  - 問題: アニメーション: 時間 0.2s、イージング ease（既定）
  - 修正: イージング=ease（cubic-bezier(0.25,0.1,0.25,1)）と明示固定する
- **[s3|検証/実機裁量]** 5-3-6 D / hover の Free可否
  - 問題: Free可否は未検証（§10.7）→ Mini切替後に有効化前提でも視覚劣化は浮き上がり消失のみ。
  - 修正: 対象プランを固定し（例: Miniで実装）hoverは必ず有効化、と確定する
- **[s3|判断語]** 5-3-6 B-2 手順10 / 右列gap
  - 問題: Card Right Col 内に Box をドラッグ→Card Right Col と命名。… gap 14px。
  - 修正: 5-3-5注の『カード幅%を48%前後に手動調整』は非推奨明記済だが、参考記述ごと削除し既定方式のみ残すと裁量余地が消える
- **[s4|推奨止まり]** 5-4-2 ステップ6 sub項目（market-inner方式）
  - 問題: Studioでは market-inner を幅 %100%にし、子の market-head／market-grid 各々に最大幅を持たせる方式とします。
  - 修正: 本ステップで中央寄せの具体手段（左右margin auto）まで明記し参照を閉じる。
- **[s4|判断語]** 5-4-3 callout tip（word-break）
  - 問題: Studio側で「単語で折返さない」相当の設定があれば有効化、無ければ最大幅で折返し位置を制御（実害は軽微）。
  - 修正: 「keep-all相当設定は使用しない。折返しは最大幅820/760pxで制御する」と一方に確定する。
- **[s4|推奨止まり]** 5-4-7 callout warn（見出しレスポンシブ改行）
  - 問題: 推奨: PC/Tablet/Mobileとも market-title は1テキストのまま、最大幅で折返し位置を制御します。
  - 修正: 「market-titleは全BPで1テキストのまま最大幅制御とする（必須）」と断定。
- **[s4|任意・省略可]** 5-4-2 callout note（Free段階暫定単色）
  - 問題: 暫定として塗り = 単色 white（中央帯の色）にしておき、上下の薄青グラデはMini切替後に上記いずれかで付与する。
  - 修正: 実装段階（プラン）を固定し、暫定ではなく最終形を直接作る手順に統一。
- **[s5|選択肢未確定]** 5-5-2 間隔（Gap）
  - 問題: 見出しとリードの間隔は本文側の margin で取るためここは 0（または下記 svc-title の下マージン24pxで吸収）。
  - 修正: 「Gap=0とし、間隔は svc-title の下マージン24pxで取る」と一方に確定する。
- **[s5|選択肢未確定]** 5-5-4 svc-meta-1 テキストスタイル適用
  - 問題: テキストスタイル English Label を適用、または個別に → フォント Inter、ウェイト 700…
  - 修正: 「テキストスタイル English Label を適用する（未登録なら先に登録）。個別直指定はしない」と一方に確定する。
- **[s5|判断語]** 5-5-6 checklist
  - 問題: コネクタの扱い（SVG重ね／省略／Mini Custom Code）を選び、PCで意図どおり表示されることを確認した
  - 修正: 確定方式を前提に「Box2枚コネクタがPCで中央に十字表示される」と客観条件で書く。
- **[s5|曖昧値]** 5-5-7 図 figcaption / 本文
  - 問題: 対応方針: 1024px以下の調整 → Tablet（840px）、767px以下の調整 → Mobile（540px）。
  - 修正: 「Tablet段（840px幅で編集）で上書き／Mobile段（540px幅で編集）で上書き」とStudio段のみで記述し、1024/767の閾値表記は説明注に限定する。
- **[s5|検証/実機裁量]** 5-5-7 Mobile svc-title
  - 問題: テキストスタイル H2 Section が1スタイル内にMobile値（32px）を内包していれば、ここでの個別上書きは不要。内包していなければ Mobile で 32px を指定します。
  - 修正: 「H2 SectionにMobile 32pxを内包させたうえで個別上書きはしない（内包設定を必須手順化）」と一本化する。
- **[s5|選択肢未確定]** 5-5-7 Mobile svc-grid 1カラム化
  - 問題: 子カード幅を 100%（または flex 折返しで1列）にする。
  - 修正: 「子カード幅を100%にする」と一方に確定する。
- **[s5|選択肢未確定]** 5-5-4 overflow
  - 問題: overflow は visible（コネクタをはみ出させるため）。コネクタを別レイヤー画像化する方針（5-5-6）なら overflow は既定でよい。
  - 修正: コネクタ方式を確定（Box2枚）したうえで「overflowは既定のままでよい（visible指定不要）」と一意に書く。
- **[s5|推奨止まり]** 5-5-2 svc-title Mini
  - 問題: Custom Code（Head）に #service .svc-title{word-break:keep-all} を注入して…推奨は Free 代替で十分です。
  - 修正: プラン方針で1つに確定（例: Freeで実装しMiniは行わない）と言い切る。
- **[s5|選択肢未確定]** 5-5-8 完了チェック 実機確認
  - 問題: 公開URLで実機確認（グラデ・カード折返し・コネクタ表示／非表示は Live Preview / 公開URL での確認を必須とする）
  - 修正: 「公開URLで確認する」と確認手段を1つに確定する（Live Previewは可とせず）。
- **[s6|任意・省略可]** 5-6-8 仕上げチェック
  - 問題: アニメーション無し（出現演出の指定なし）。出現演出を足す場合のみ任意で On Appear・Free可否未検証）
  - 修正: 「アニメーションは付けない」と断定し、出現演出に関する任意記述を削除する
- **[s6|検証/実機裁量]** 5-6-3 手順8
  - 問題: Mobile/Tablet 実効は 5-6-7 で確認
  - 修正: Tablet/Mobile の見出しサイズ確定値（px）を本手順内に直接併記し、確認依存をなくす
- **[s6|選択肢未確定]** 5-6-5 脚注 / 5-6-7 写像方針（数字サイズ）
  - 問題: Mobile に統合（数字 38px/ラベル 12.5px 採用）
  - 修正: Mobile確定値を 数字38px/ラベル12.5px に断定し、Mini分岐の併記を削除して採用値を一意化する
- **[s6|曖昧値]** 5-6-4 Free代替（数字単位small）
  - 問題: 前マージン 3px 相当
  - 修正: 単位smallの前マージンを『left margin 3px』のように適用プロパティと値を一意に確定する
- **[s6|検証/実機裁量]** 5-6-6 手順1
  - 問題: URLの取得方法は §0.4「アップロードした画像の URL を取得する」（配置→プレビュー→検証でコピー。Custom Code 主案で使用）
  - 修正: 本章手順としてURL取得の確定ステップを明記（または取得後にCSSへ貼る手順を一意に記述）し、プレースホルダ運用を確定化する
- **[s7|任意・省略可]** 5-7-4 B 手順6 / 5-7-7
  - 問題: 彩度（saturate(.95)）はほぼ等倍のため省略可。厳密再現が必要な場合のみ画像タブのフィルタで Saturation を 95% に。
  - 修正: 「Saturation は設定しない（saturate(.95)は適用不要）」のように一意に決める（省略で統一）。
- **[s7|任意・省略可]** 5-7-4 A-2 手順3
  - 問題: 方向（Direction）：任意（子は画像1つ）。
  - 修正: 「方向（Direction）：縦並び（Vertical）固定」など1つに指定する（中央配置を確実にするため値を確定）。
- **[s7|検証/実機裁量]** 5-7-6 改行制御 / チェックリスト
  - 問題: 最終は Live Preview / 公開URLで実機確認。 … Live Preview（実機スマホ）でタイトル3行・5→3→2列グリッド・vc-big表示を確認した
  - 修正: 確認はチェックのみとし、「3行に収まらない場合は vc-title 幅820pxを維持しフォントを変えず改行Enter位置のみ既定どおり」など、ずれ時の確定対処を1つ明記する。
- **[s7|検証/実機裁量]** 5-7-2 直後 tip
  - 問題: Freeで進める場合は Live Preview で黄帯の位置とベースラインを実機確認すること。
  - 修正: 黄帯のストップ（62%/90%）と横並びBoxの垂直配置＝下端を固定値として明記し、確認は合否チェックのみに留める（裁量調整を排除）。
- **[s8|検証/実機裁量]** 5-8-1 Free代替/Mini
  - 問題: 再現が崩れる場合のみ Custom Code で background:linear-gradient(...) を当てる。ネイティブで足りるため非推奨。
  - 修正: 判定基準（例:公開URLで指定3ストップと差異が出たら必ずCustom Code）を数値/手順で固定するか、最初からどちらか一方に統一する。
- **[s8|任意・省略可]** 5-8-4 リザルト warn
  - 問題: 和字グリフが無い場合フォールバックする点に注意（5-5.4）。崩れるなら単位のみ Noto Sans JP 12px に変更可（視覚差は軽微）。
  - 修正: 「単位『万円』は Noto Sans JP 12px にする」と最初から一律確定する（または必ず Inter のままと断定）。
- **[s8|検証/実機裁量]** 前提 callout note（冒頭）
  - 問題: aspect-ratio / flex-shrink / line-break を Studio 標準 UI で個別設定できない場合は各所の【Free代替】/【Mini】に従います。
  - 修正: 各プロパティについて本書時点でのUI有無を確定記載し、採るルートを1つに固定する。
- **[s8|判断語]** 5-8-4 Industry スタイル
  - 問題: margin は明示リセットなし（Studioではテキスト既定の上下margin0で運用）。
  - 修正: 「margin 上下=0 を明示設定する」と確定手順で書く。
- **[s8|曖昧値]** 5-8-4 業種タグ Industry コピー
  - 問題: Pre-Aラウンド · 約25名
  - 修正: 確定コピーとして問題なし（除外可）。表示文字列は一字一句固定の前提を明記すれば足りる。
- **[s8|判断語]** 5-8-4 Quote 手順5 サブ項目
  - 問題: 折返しは Studio標準でよい（鉤括弧はコピー文字列に直接含める）。
  - 修正: 「折返しは word-break=normal（既定）のまま、明示変更しない」と確定する。
- **[s8|任意・省略可]** 5-8-5 Mobile callout note
  - 問題: Mini/Mobile（320）に独立ブレークポイントを追加する場合（§8.1 任意追加）でも…Mobile(540) の値をそのまま継承させる（追加不要）
  - 修正: 「320 の独立BPは追加しない」と一律に確定する。
- **[s9|推奨止まり]** 5-9-5b / faq-q-arrow-1 のプラン分岐（Free代替 callout）
  - 問題: Studio は擬似要素非対応のため、Icon（chevron-down）で作り、Toggle の Closed 条件スタイルで回転させます（推奨）。
  - 修正: 「矢印は Icon（chevron-down）で作り Closed 条件スタイルで回転させる（Custom Code は使わない）」と断定する。
- **[s9|曖昧値]** 5-9-5b 手順16 / faq-q-arrow-1 線幅
  - 問題: ストローク色 カラースタイル blue（#4A7DE8）／ 線幅 約 2.5px
  - 修正: 「線幅 2.5px」と『約』を削って単一値に確定する。
- **[s9|任意・省略可]** 5-9-5b 手順13 / faq-q-text-1 hover
  - 問題: トリガー対象は質問行（faq-q-1）の Hover で文字色を変える運用でも可。
  - 修正: どちらか一方（例：faq-q-text-1 自身の Hover State で色を blue に変える）に確定し、代替運用の記述を削除する。
- **[s9|判断語]** 5-9-3 手順8 / faq-title 改行制御
  - 問題: Studio では faq-title の幅を中央寄せ 820px 内で調整し、PC（≥1280px 相当）と Mobile では 1 行寄り、Tablet では 2 行になるよう、Tablet 条件スタイルで改行を入れます
  - 修正: 改行は条件スタイルで明示改行を入れる箇所のみで制御すると規定し、幅は 820px 固定（手動調整禁止）と明記。PC/Mobile は改行を入れない、Tablet はフレーズ間に明示改行1つ、と手順を一意化する。
- **[s9|検証/実機裁量]** 5-9-7 手順1 / 最終確認
  - 問題: Toggle 開閉はエディタ内プレビューでも確認できるが、最終はライブプレビュー／公開 URL で実機確認する
  - 修正: 確認の合否基準（初期 open=item-1 のみ、open時 border=blue/lt・影=0 16px 36px、矢印=180°）を数値で列挙し、目視ではなく値一致で判定すると規定。
- **[s9|検証/実機裁量]** 5-9-7 手順5 / 最終確認
  - 問題: 質問テキストとバッジの縦位置が揃っていること。
  - 修正: 「faq-q-badge の margin-top -2px が設定済みであること」と確定値の有無で判定する形に置換する（目視微調整を求めない）。
- **[s9|判断語]** 5-9-2 手順4 ほか各所 / padding 表記の前提
  - 問題: 余白（Padding）→ 左右 40px（ガター）、上下 0。
  - 修正: —
- **[s10|選択肢未確定]** §5-10-1 手順4
  - 問題: 幅 Fill（または % = 100%）
  - 修正: 「幅 = Fill に設定（% を使う場合は 100%）」ではなく「幅 = Fill」のみに確定する
- **[s10|選択肢未確定]** §5-10-2 plan mini
  - 問題: Custom Code（body）に <div> ＋上記 CSS を書いて再現も可だが、装飾のため不要。
  - 修正: 「Mini でも Custom Code は使わず Free 代替の Box を流用する」と単一手順に確定する
- **[s10|選択肢未確定]** §5-10-4 B チェックリスト
  - 問題: 改行方針 (a) 改行なし／(b) 2Text 分割 のどちらを採るか確定済み（既定: (a)。…）
  - 修正: 「(a) 改行なしで作成済み」に固定し選択の余地を削除する
- **[s10|推奨止まり]** §5-10-5 C しっぽ／plan free recommended
  - 問題: （推奨）しっぽ無しの角丸吹き出しで確定（装飾上の影響は軽微）。
  - 修正: 「しっぽ無しの角丸吹き出しとする（厳密再現は行わない）」と Mini 案を排他的に閉じる
- **[s10|曖昧値]** §5-10-6 A 手順2
  - 問題: フォーム名を Contact 等に設定
  - 修正: 「フォーム名を Contact に設定」と単一の固定文字列にする（『等』を削除）
- **[s10|推奨止まり]** §5-10-4 B (a) plan free
  - 問題: (a) 改行なし・全 BP 自然折返し（推奨・本手順の既定）
  - 修正: 「本手順は (a) を採用する。(b) は行わない」と断定する
- **[s10|判断語]** §5-10-3 手順3
  - 問題: 水平中央寄せ（margin 左右 auto 相当）
  - 修正: Studio 上の具体操作（例: 配置→水平 中央 を選択、または margin 左右を Auto に設定）を1つ明記する
- **[s10|判断語]** §5-10-5 B 手順3
  - 問題: Move X -50%相当（左へ自幅半分）
  - 修正: 変形タブで Move X に -50% を入力（％指定不可なら -94px と固定値）と一意に指定する
- **[s10|任意・省略可]** §5-10-7 Mobile 手順2
  - 問題: 本来 24px ですが Studio Mobile は単一段のため 28px を採用（必要なら Mini BP 320px で 24px 化）
  - 修正: 「Mobile 見出しは 28px で確定。320px 用 BP は追加しない」と一意に固定する
- **[s10|検証/実機裁量]** §5-10-9 未確定・注意／541〜767px
  - 問題: 768px ちょうどに閾値を置く厳密再現が必要なら Tablet 境界調整または追加 BP を設計（任意・要デザイン確認）。
  - 修正: 「閾値調整・追加BPは行わず 541〜767px の見出し差は許容で確定」と明記し未確定を解消する
- **[s10|検証/実機裁量]** §5-10-9 未確定・注意／狭幅
  - 問題: 320px 実機が崩れる場合のみ Mini BP（320px）を追加します。
  - 修正: 320px 実機確認を本手順内の必須ステップ化し、判定基準（崩れ＝何px超過等）と追加要否を事前確定する
- **[s10|推奨止まり]** §5-10-6 A フォーム背景グロー／plan mini
  - 問題: Custom Code で ::before/::after 再現。非推奨。
  - 修正: 「Mini でも Custom Code は使用しない」と明確に禁止する
- **[s10|判断語]** §5-10-6 G 手順1
  - 問題: Submit の直後に Text（リッチテキスト）を配置
  - 修正: 「リッチテキスト要素を配置」と一意に指定し、通常 Text を排除する
- **[s11|曖昧値]** §2 警告callout（gap考慮の注意）
  - 問題: 左カラム幅は gap込みで約500pxが正解です。
  - 修正: 「496px」と単一値で記載。
- **[s11|検証/実機裁量]** §1 手順2 sec-footer のタグ設定
  - 問題: タグ設定で footer を選択（選択肢にある場合）。
  - 修正: 「footerが選択肢に無い場合はdivのまま進める（または設定タブ→カスタムタグにfooterと入力）」と分岐を確定手順化。
- **[s11|任意・省略可]** §1 手順8 既定文字色
  - 問題: 既定文字色は子要素側で個別指定するため、ここでは設定不要（必要なら ink を設定）。
  - 修正: 「sec-footerの文字色は設定しない（子で個別指定）」と一方に固定し、任意条項を削除。
- **[s12|選択肢未確定]** §5-12-4 step2 幅（行346）
  - 問題: 幅 auto（親幅いっぱい・縦並び親では交差軸ストレッチ）または幅 %100、高さ auto
  - 修正: 「幅は %100（width:100%）に設定」と1つに確定する
- **[s12|選択肢未確定]** §5-12-4 グロー円 Free代替（行366）
  - 問題: Studio に放射グラデ（radial）UIが無い場合は Image化（PNG・透過放射ぼかし円を…配置）。
  - 修正: Studioの放射グラデUI有無を事前確定し、どちらの手法を採るかを一意に指定する（PNGなら配置するPNGの素材パスも指定）
- **[s12|任意・省略可]** §5-12-7 Free代替（行437）
  - 問題: 色を strike-red にしたい場合は該当文字色を strike-red にして打消し線色を寄せる。
  - 修正: 「該当文字色を strike-red(#FF6B6B) にして打消し線色を寄せる」を必須手順として断定する
- **[s12|任意・省略可]** §5-12-8 drop-shadow Free代替（行492, 498）
  - 問題: Big配下の「0」にあたる Text に Shadow（Outset）を付与で近似…または装飾省略。Free段階は省略可。
  - 修正: Free段階での扱いを「Shadow（Outset）0/4/12 rgba(232,154,28,.35)を付与する」または「省略する」のどちらかに断定する
- **[s12|選択肢未確定]** §5-12-8b perkShine Free代替（行530）
  - 問題: 省略するか、白半透明の細Boxを On Appear で一度だけ流す簡略表現に留める。
  - 修正: 「Free段階では perkShine は省略する」と1つに確定（または簡略Boxを入れると断定し、Boxの寸法・色・タイミングまで指定）
- **[s12|任意・省略可]** §5-12-1 タグ補足（行153）, §5-12-2 設定（行239）
  - 問題: Studio でタグ変更不可なら Box で代替し視覚・動作上の支障はなし（必要なら 設定タブ→属性 で role="complementary" を付与してセマンティクスを補える）
  - 修正: 「Boxで代替する場合は role="complementary" を必ず付与する」と断定する
- **[s12|検証/実機裁量]** §5-12-11 写像注記（行678）
  - 問題: 最終はライブプレビュー/公開URLで実機確認（fixed/出現JSはエディタ内で再現しない場合あり…）
  - 修正: 確認は手順として残しつつ、ずれた場合の対処を「§5-12-11の表の値を再適用する」等の確定手順に限定し、裁量調整を排除する
- **[s12|推奨止まり]** §5-12-2 Free代替 設定（行229）
  - 問題: プリセット Fade In from Bottom（=下から 16px せり上がりながら…）。アニメ時間は…時間 0.3s、イージング ease。
  - 修正: Free段階の出現は「On Appear / Fade In from Bottom / 0.3s / ease を必ず設定する」と断定する
- **[t6|判断語]** 6.3.1 [A] 手順3
  - 問題: ロゴ列は品よく 40〜60px/s で調整
  - 修正: 「品よく」を削除し、確定値60px/sを指定。
- **[t6|判断語]** 6.3.4 [G] callout note
  - 問題: 不確実性を避けるなら On Appear に留める。
  - 修正: 「Scroll Effectは使わずOn Appearに留める」と断定。
- **[t6|推奨止まり]** 6.4.1 [H] callout tip
  - 問題: 推奨: Free段階＝静的ドット、Mini切替後＝Custom Code で点滅。
  - 修正: 「〜にせよ」と指示形に変える（推奨ではなく確定指示）。
- **[t6|推奨止まり]** 6.4.2 [I][J] callout tip
  - 問題: 推奨: ヒーロー見出し一式を1つの Embed sandbox 化（…）。
  - 修正: 「ヒーロー見出しはEmbed sandboxで実装する」と指示形で言い切る。
- **[t6|推奨止まり]** 6.4.3 [K][L] callout tip
  - 問題: 推奨: フローティングバナーごと Embed sandbox 化し perkShine を同梱（…）。光沢が不要なら静的な太字「0」でも
  - 修正: 「Embed sandbox化しperkShineを同梱する（光沢は必須）」と確定し、不要時条項を削除。
- **[t6|判断語]** 6.4.3 [K][L] 推奨
  - 問題: 光沢が不要なら静的な太字「0」でも訴求は維持される（nice-to-have）。
  - 修正: 光沢を入れる/入れないをプロジェクトとして確定し条件文を排除。
- **[t6|任意・省略可]** 6.1 callout warn
  - 問題: これらの死にコードの掃除は別タスクで実施してよいが、本ガイドの対象外。
  - 修正: 「本作業では掃除しない（別タスク管理）」と明記。
- **[t6|判断語]** 6.3.3 [E] フォーム input
  - 問題: フォーム 全体の構成は 5-8 系セクションと整合させる。
  - 修正: 整合すべき具体項目（参照する確定値・節）を列挙して手順化。
- **[t6|検証/実機裁量]** 6.3.4 [G] callout note / Scroll Effect
  - 問題: 公開時は現行（安定）インフラでPublishしていることを確認。
  - 修正: 確認手順を具体化（どこで何を確認しNGなら何をするか）するか、Scroll Effect不採用で項目を消す。
- **[t7|任意・省略可]** §7.4.4 補足（休眠境界線）
  - 問題: スクロール時に 1px 下境界線を出す挙動は…Studio で再現するならスクロール連動インタラクションが要りますが、省略可。
  - 修正: 「再現しない（境界線は出さない）」と一意に決める
- **[t7|任意・省略可]** §7.7.1 note
  - 問題: Tablet を触ると下方カスケードで Mobile にも 64 が乗るが、結果同値なので Tablet で 64 にしても可。簡潔さ優先で Mobile のみで処理
  - 修正: 「Tablet は触らず Mobile のみで padding 64 を当てる」と一意に固定する
- **[t7|任意・省略可]** §7.13.2 手順4
  - 問題: 装飾光 Box → 90×90・110×110 に縮小（または非表示でも可）。
  - 修正: 「90×90・110×110 に縮小する（非表示にしない）」のどちらかに確定する
- **[t7|任意・省略可]** §7.14（warn）
  - 問題: Mobile で左右 padding が広すぎる場合は手動で縮小可。
  - 修正: Mobile のフッター左右 padding の確定値を指定する（縮小する/しないを一意に）
- **[t7|推奨止まり]** §7.7.2 チェックリスト / §7.5.2 補足
  - 問題: 矢印グリフ（↘/↗）の字形が Mobile でも崩れない（Icon/SVG 推奨）
  - 修正: 「矢印は SVG/Icon にする」と必須化する
- **[t8|検証/実機裁量]** §8.0.1 IncubateFund行 対処
  - 問題: カード上でぼやける可能性。Studio 配置後に鮮明さを目視確認
  - 修正: 「目視でぼやけたら長辺を変えず元534×94のまま採用（許容）」など合否基準と対処を1つに固定する
- **[t8|曖昧値]** §8.0.2 手順4
  - 問題: アップローダがファイル名を化けさせる／拒否する場合は BoosterCapital01.jpg 等にリネームしてから上げ直す
  - 修正: リネーム後の名前を「BoosterCapital01.jpg」に固定指定（化けた場合は必ずこの名前）
- **[t8|曖昧値]** §8.3 静的グリッド手順3 / 本文
  - 問題: 彩度をわずかに下げる（saturate 0.95 相当）
  - 修正: Saturation スライダーの入力値を単一に明記（例: 95% / 0.95）し「軽く」を削る
- **[t8|選択肢未確定]** §8.5 Mobile/Tablet条件スタイル
  - 問題: レイヤーパネルの目アイコンで非表示、または条件スタイルで該当 BP の表示を切る
  - 修正: 「Mobile・Tablet の条件スタイルで display を切る方法に統一」と1手段に確定する
- **[t8|検証/実機裁量]** §8.8 Studio手順 手順4
  - 問題: center 35% の切り取りが許容範囲か Studio 上で目視確認
  - 修正: 「位置は center 35% で固定。目視で再調整しない」と確定し、合否判断を排除する
- **[t8|検証/実機裁量]** §8.8 callout
  - 問題: 遅延読み込みの自動付与可否は confidence: 未検証。実機で要確認
  - 修正: 「lazy は新公開基盤の自動付与に委ねる。Studio側で追加設定しない」と方針を1つに固定する
- **[t8|曖昧値]** §8.1 A案 手順3 / §8.2 手順2
  - 問題: 幅 auto（最大 180px 相当）、高さ 140px
  - 修正: 「ボックス幅＝max-width 180px、高さ140px」と入力欄と値を1対1で明記する（「相当」を削る）
- **[t9|検証/実機裁量]** §9.7 / §9.9 / 各checklist
  - 問題: これら（Hover/Toggle/On Appear）の Free 可否は未検証のため Mini 切替後有効化前提と明記した
  - 修正: 「Free 期間は一切設定せず、Mini 切替後にまとめて設定する」と手順を一本化する。
- **[t9|任意・省略可]** §9.1 機能・装飾表（5-12 装飾円 ほか）
  - 問題: <strong>【Free】</strong>装飾 Box / 透過 PNG。省略可。
  - 修正: 「装飾 Box で実装する」または「省略する」のどちらかに統一して言い切る。
- **[t9|選択肢未確定]** §9.1 機能・装飾表（5-1 ヘッダー チッカ）
  - 問題: <strong>【Free】</strong>赤キャプション＋チッカを1枚の SVG/PNG 画像化（モバイルで非表示）。または極小回転 Box×2。
  - 修正: どちらか一方（例：画像化）に確定し「または…」を削除する。
- **[t9|任意・省略可]** §9.6 アニメ表（blink・Free代替）
  - 問題: <strong>静的ドット</strong>（点滅省略・視認性影響小）
  - 修正: 「Free 期間は静的ドット（点滅なし）で確定。Mini 切替後に Custom Code で点滅付与」と一意化（既に近いが言い切りを明確化）。
- **[t9|選択肢未確定]** §9.6 アニメ表（赤斜線取消線・Free代替）
  - 問題: 細い赤 Box（高さ<code>1.4px</code>…）をテキスト上に absolute＋<code>rotate(-5deg)</code></strong>、または画像化。
  - 修正: 「5-12 を Embed 化する場合は同梱、それ以外は傾き赤 Box で実装（画像化はしない）」と分岐を一意化。
- **[t9|判断語]** §9.5 Mini 行
  - 問題: どうしても <code>mask-image</code> フェードを CSS で厳密再現したい場合のみ、Custom Code に <code>.lw-marquee{mask-image:...}</code> 相当を注入
  - 修正: 「両端フェードは重ねグラデ Box で固定。Custom Code の mask-image は使用しない」と言い切る。
- **[t9|検証/実機裁量]** §9.5 手順1
  - 問題: <code>01BoosterCapital.jpg</code> は<strong>先頭が数字</strong>のため、アップローダがファイル名を変形しないか確認（変形時はリネーム）。
  - 修正: リネーム後の確定名（例：BoosterCapital01.jpg）を指定し、参照箇所も合わせて固定する。
- **[t9|推奨止まり]** §9.3 / §9.9 推奨欄
  - 問題: Mini 切替後 … Embed 継続 or Custom Code body
  - 修正: 「Mini 切替後も Embed を継続（Custom Code へは移さない）」と確定する。
- **[t10|曖昧値]** 10.1 表示確認 PC
  - 問題: ブラウザ幅 1920px で全セクション（…）に横スクロール・はみ出しなし
  - 修正: 判定は「横スクロールバーが出ない＝document.body.scrollWidth ≤ ビューポート幅」を合格基準と明記し、各幅で同一判定文にする。
- **[t10|検証/実機裁量]** 10.1 表示確認 PC
  - 問題: vc-marquee / lw-marquee（Loop Box）が左方向にスムーズに流れる（速度 60px/s 既定、設計値どおりか確認）
  - 修正: 「Loop Box の速度設定値が 60px/s になっていることをエディタの数値欄で確認」と数値一致チェックに変え、体感判定を排除する。
- **[t10|判断語]** 10.4 タッチ・操作性
  - 問題: スクロール中にちらつき・カクつきが目立たない
  - 修正: 判定対象を「スクロール時に固定ヘッダーの再描画ちらつき・1秒以上続くカクつきがない」等の観測可能事象に限定して記す。
- **[t10|検証/実機裁量]** 10.7 メタ
  - 問題: Google Search Console に sitemap を登録（クライアント GSC 連携可否を確認）
  - 修正: GSC連携の可否と担当（クライアント/制作側）を事前確定し、「登録する」か「クライアント側対応で本リスト対象外」を明記する。
- **[t10|曖昧値]** 10.7 公開基盤
  - 問題: 新基盤を選択 → 保存（約5分）
  - 修正: 「保存後5分待機してから再検証（再検証前に generator=Studio.Design.HRC が出ることを再確認）」と完了条件を手順化する。
- **[t10|推奨止まり]** 10.9 アクセシビリティ
  - 問題: フォームの各ラベルと Input が関連付いている（ラベル位置「上」推奨、「非表示」にしない）
  - 修正: 「ラベル位置は上に固定（左/プレースホルダ内は不可）」と断定形にする。
- **[t10|曖昧値]** 10.10 パフォーマンス
  - 問題: VC ロゴ画像が圧縮済み（各 100KB 以下目安）
  - 修正: 「各VCロゴ画像 ≤ 100KB（超過は不合格、再圧縮）」と上限を確定値にする。
- **[t11|選択肢未確定]** 11.4b 2カードの gap・padding 縮小
  - 問題: カード幅を % または flex で持たせ
  - 修正: 「カード幅は flex:1 で持たせる」等、片方に確定する
- **[t11|任意・省略可]** 11.5a 十字点線コネクタ Free代替
  - 問題: 情報伝達上は装飾なので省略可（低リスク）
  - 修正: 「十字コネクタは省略する（Mobileは元々非表示／PCも画像化しない）」等、実装可否を一方に確定
- **[t11|選択肢未確定]** 11.7a 対処
  - 問題: vc-card の aspect-ratio:5/2 は…無ければ固定高 or パディングハック
  - 修正: 「aspect-ratio が無い場合は固定高 XXpx を使う」とフォールバックを一意に決める
- **[t11|選択肢未確定]** 11.10a CTA Free代替
  - 問題: イラスト群は 1 枚 SVG/PNG にベイクし Image 配置
  - 修正: ベイク画像のフォーマットを SVG か PNG に統一して明記する
- **[t11|任意・省略可]** 11.12b チェックリスト
  - 問題: 「0」グラデ＋シャイン（perkShine）は Embed で再現 or 静止グラデ画像で妥協（装飾性高く省略可）
  - 修正: 「0のシャインは静止グラデ画像で実装」等、単一手段に確定する
- **[t11|任意・省略可]** 11.13-6 横断チェック表
  - 問題: カード hover の translateY(-4px)：Studio hover で再現 or 省略
  - 修正: 「hover の translateY(-4px) は再現する」または「省略する」を一方に確定
- **[t11|検証/実機裁量]** 11.4a px固定高棒グラフ
  - 問題: Mobile で mkt-chart{height:190px}（PC210）
  - 修正: Mobile でも棒高は150/70/66/152pxのまま据置と明記（実質確定済みなら追記不要）
- **[t11|推奨止まり]** 11.9b アコーディオン Mini代替
  - 問題: 完全一致が要れば <details> を Custom Code Body 注入
  - 修正: 本LPは「Toggleで実装する（detailsは使わない）」と言い切る
- **[t11|選択肢未確定]** 11.5a Free代替
  - 問題: 十字点線を 1 枚の SVG/PNG にしグリッド中央へ重ね
  - 修正: 全装飾アセットの書き出し形式を SVG か PNG に統一して明記
- **[t11|選択肢未確定]** 11.11a フッター対処
  - 問題: Studio では空 Box の 2 列 Grid か、単純に左寄せ Box＋max-width 制御
  - 修正: 「ft-inner は左寄せ Box＋max-width で作る」等、片方に確定する
- **[t11|選択肢未確定]** 11.6b 月桂樹
  - 問題: Studio では Image 2 枚（左反転）か背景画像化
  - 修正: 「月桂樹は laurel.svg を Image 2 枚（右は水平反転）で配置する」と一方に確定