# CLAUDE.md — Agent 強制ルール（最小核）

**このファイルは Claude Code CLI が自動ロードする運用ルール抜粋です。**
詳細・全規範は [`AGENT.md`](AGENT.md) を必ず読んでください。本ファイルは「最低これは守れ」の核だけを記載します。

---

## 🛑 絶対遵守ルール（違反すると並行運用が崩壊する）

### Rule 1. ファイル編集の前後で必ず activity_log.md に記録

| タイミング | タグ | 必須内容 |
| --- | --- | --- |
| セッション開始時 | `[SESSION-START]` | 自セッション識別子＋直近予定 |
| ファイル編集の**直前** | `[INTENT]` | 対象ファイル＋これからの変更内容 |
| ファイル編集の**直後** | `[DONE]` | 対象ファイル＋実施した変更の要約 |
| ブロッカー発生 | `[BLOCKER]` | 何が止まっているか・誰待ちか |
| 他セッション引き継ぎ | `[HANDOFF]` | 引き継ぎ事項 |
| セッション終了時 | `[SESSION-END]` | 残作業サマリ |

**「ユーザーから短い指示が来たから手順を省略する」は禁止。**
**「軽微な修正だから記録しない」も禁止。**
記録漏れは他セッションのコンテキストを断絶させ、プロジェクトを壊す。

### Rule 2. セッション開始時の必須シーケンス（このまま実行）

ユーザーから最初の指示を受ける前に、必ず以下の順序でファイルを読む:

1. `README.md` を読む
2. `AGENT.md` を読む（特に §5 / §7 / §9 / §12）
3. `activity_log.md` の直近 20 エントリを読み、他セッションの活動と自セッションの直近状態を把握
4. ユーザーにセッション識別子を確認（既存と衝突しないもの）
5. `[SESSION-START]` エントリを `activity_log.md` に追記
6. `tasks.md` / `questions.md` / `notes_input.md` / `design_rules.md` を順に読む
7. **Gemini セットアップ確認**: `activity_log.md` で「自セッション識別子の Gemini setup [DONE]」を grep。なければ [`setup_gemini.md`](setup_gemini.md) を実行
8. 残タスクと未回答質問の要約をユーザーに提示

### Rule 3. ファイル編集前のチェック（毎回）

1. 対象ファイルを Read で**最新状態に読み直す**（他セッションが触っているかも）
2. `activity_log.md` で他セッションの直近 `[INTENT]` を確認（30 分以内なら待つかユーザー確認）
3. `[INTENT]` を `activity_log.md` に書く
4. 編集する
5. `[DONE]` を `activity_log.md` に書く

### Rule 4. ディレクトリ境界

| ディレクトリ | 権限 |
| --- | --- |
| `01_input/` | **読み取り専用**。絶対に変更しない |
| `02_work/` | 自由に編集可（ただし上記ルール下で） |
| `02_work/mockup/drafts/minon/` | **kasahara の作業領域**。azalea セッション側は参照のみで上書き・削除しない |

### Rule 5. モックアップ作成時の命名

- ドラフト: `v{連番}_{YYYYMMDD}_{対象}_{短い説明}.html`
- 全体通し: `{対象}=full`
- 並列案: 末尾 `_A` / `_B`
- **既存ファイルを上書きしない**。常に新版番号で保存
- 作成後 `mockup/INDEX.md` の版履歴に追記

---

## 📂 主要ファイル早見表

| ファイル | 役割 |
| --- | --- |
| `README.md` | 引き継ぎエントリーポイント。最初に読む |
| `AGENT.md` | 全行動規範。本ファイルの詳細版 |
| `activity_log.md` | **セッション間活動ログ**（本ファイルが強制している記録先） |
| `tasks.md` | タスク管理台帳 |
| `questions.md` | ユーザー要確認事項 |
| `design_rules.md` | 確定済みデザイン要素（厳守対象） |
| `notes_input.md` | インプット資料の要約 |
| `mockup/INDEX.md` | モックアップ版履歴 |
| `mockup/assets/INDEX.md` | 生成画像の管理 |
| `setup_gemini.md` | Gemini 画像生成セットアップ手順 |

---

## 🔍 違反検知時の対応（自セッション）

もし自分が編集したのに `[INTENT]` / `[DONE]` を打ち忘れたと気付いたら:

1. ただちに遡及で `[DONE]` エントリを書く（時刻は実編集時刻、本文に「遡及記録」と注記）
2. 同じセッション内で再発しないようチェックリストを使う
3. ユーザーに「記録漏れがあったので遡及記録した」と一言報告

他セッションの記録漏れに気付いたら（azalea が kasahara の漏れを発見した等）:

1. ファイル mtime と内容から推定し、遡及で `[DONE] {他セッション識別子}` を書く（本文末尾に「{自分} が遡及記録」と注記）
2. ユーザーに状況を報告し、再発防止策を相談する

---

最終更新: 2026-04-28（初版・短期再発防止施策）
